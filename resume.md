# Resume — Estado del proyecto y trabajo realizado

Última actualización: 2026-07-28 (decimosexta sesión: 014-notificaciones completado, 521/521 tests verdes). Este archivo existe para que cualquier agente (o persona) pueda retomar el trabajo sin releer toda la conversación anterior.

## Qué se hizo el 2026-07-28 (decimosexta sesión): 014-notificaciones completo (521/521 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, spec transversal: "solo define la tabla y el endpoint de marcar-leída, los triggers viven en cada feature origen"). `014-notificaciones/spec.md` estaba en `status: draft`; los criterios de aceptación eran suficientes, así que se implementó directamente y se pasó a `status: implemented`. Es la primera sesión que toca código de **6 features ya cerradas** (001, 006, 008, 010, 011, 012, 013) para engancharles el trigger correspondiente — el mayor blast radius de cualquier sesión hasta ahora, mitigado corriendo la suite completa (no solo la de la feature nueva) antes de dar la sesión por cerrada.

**Backend:**
- **Migración `notificaciones`**: `usuario_marketplace_id`/`usuario_sistema_id` FK NULL con CHECK de exactamente uno no nulo (`(a IS NOT NULL)::int + (b IS NOT NULL)::int = 1`), `taller_id`/`orden_trabajo_id` FK NULL como contexto opcional (no como tenant boundary — el destinatario ya es el usuario), `data` JSONB NULL, `leida`/`leida_at`. Sin soft delete (no es un criterio de `014-spec.md`).
- **Modelo `Notificacion`** (`scopeNoLeidas()`, `marcarLeida()` idempotente) + **2 Actions en `app/Actions/Notificaciones/`**: `CrearNotificacionAction` (único punto de escritura, valida el XOR de destinatario en PHP antes del CHECK de BD), `MarcarNotificacionLeidaAction` (valida que el actor —`UsuarioMarketplace` o `UsuarioSistema`— sea el destinatario exacto, nunca compara solo por `id` porque ambas tablas son secuencias independientes).
- **2 helpers nuevos en `UsuarioSistema`**: `conPermisoEnTaller(slug, tallerId)`/`conRolEnTaller(slugs, tallerId)` — resuelven "todos los usuarios con permiso/rol X vigente en el taller Y" a nivel de query (mismo criterio de vigencia que `UsuarioResource::getEloquentQuery()` de `008`, generalizado porque 4 de los 10 tipos de notificación lo necesitan).
- **10 clases en `app/Notifications/`** (una por tipo del catálogo de `014-plan.md`): cada una resuelve destinatario(s) + arma título/mensaje y llama a `CrearNotificacionAction`. **Decisión deliberada de no usar `Illuminate\Notifications\Notification`/`Notification::send()`** (lo que sugería `plan.md`): el canal `database` nativo de Laravel asume una tabla polimórfica `notifiable_type`/`notifiable_id`, incompatible con el esquema de destinatario mutuamente excluyente que exige `spec.md` (mismo patrón que `notas_venta_lineas` de `012`) y con `constitution.md §1` ("no existe `app/Services/`, todo es Actions"). Documentado en el docblock de `TriggersSolicitudYUsuarioTest.php`.
- **Wiring de los 10 triggers** (`app/Listeners/Notificaciones/`, `app/Observers/`, o llamada directa según el caso — sin ceremonia uniforme forzada):
  - `stock.bajo`/`pago.registrado`/`resena.nueva`: **listeners nuevos** enganchados a los eventos `StockBajoDetectado`(010)/`PagoRegistrado`(013)/`ResenaGuardada`(006), que ya se disparaban sin consumidor desde sus sesiones originales (patrón "escritor futuro" documentado explícitamente en esas 3 sesiones). `resena.nueva` distingue creación de edición/moderación con `$resena->wasRecentlyCreated` (sin tocar la firma del evento).
  - `orden.asignada`/`orden.cambio_estado`: **`OrdenTrabajoObserver`** (`created()`/`updated()`) — primer Observer del proyecto (hasta `013` todo vivía en Actions). Se eligió Observer y no un hook en cada Action porque `empleado_asignado_id`/`estado` cambian por más de un camino: `CrearOrdenTrabajoAction`, edición estándar de Filament sin Action dedicada (`011-plan.md`), `CambiarEstadoOrdenAction`, `AnularOrdenTrabajoAction`. Registrado vía atributo `#[ObservedBy(...)]` (Laravel 11+), sin tocar `AppServiceProvider`.
  - `nota.emitida`: **`NotaVentaObserver`** (`created()` únicamente — toda nota nace `EMITIDA`).
  - `solicitud.aprobada`/`solicitud.rechazada`/`usuario.creado`: **llamada directa** dentro de la Action de origen (`AprobarSinCompletarSolicitudAction`/`AprobarYCompletarSolicitudAction`/`RechazarSolicitudAction`/`VincularAccesoEmpleadoAction`) — a diferencia de los 3 anteriores, acá no había ningún evento previo esperando consumidor, así que no se agregó uno solo para tener simetría.
  - `password.expirada`: único trigger basado en el paso del tiempo, no en una acción de usuario. **Comando `notificaciones:passwords-por-vencer`** (`app/Console/Commands/`), programado diario vía `Schedule::command(...)->daily()` en `routes/console.php`. Evita duplicar la notificación cada día de la ventana de 7 días comparando contra `password_changed_at`.
- **Endpoint `POST /notificaciones/{id}/marcar-leida`** (`NotificacionController`, `routes/web.php`, guard `web,sistema` combinado — un solo endpoint para ambos lados en vez de duplicarlo, la propiedad real la valida `MarcarNotificacionLeidaAction`, no el guard).
- **2 bugs reales encontrados por los tests, no por lectura de código** (mismo patrón que sesiones `005`/`006`/`010`/`011`, ambos en manejo de excepciones no-JSON/JSON, agregados a `AppServiceProvider::boot()`/`bootstrap/app.php`):
  1. `Illuminate\Auth\Middleware\Authenticate` y `Illuminate\Auth\AuthenticationException` tienen **cada una su propio `redirectUsing()` estático** — el override que `006` ya había registrado (`Authenticate::redirectUsing(...)`) solo cubre el caso `!$request->expectsJson()`; para cualquier `fetch()` con `Accept: application/json` contra una ruta fuera de `/api/*` (como el endpoint nuevo), el middleware pasaba `null` explícito y la excepción caía en su propio fallback no-registrado, intentando `route('login')` (inexistente) → 500. Se agregó el segundo `AuthenticationException::redirectUsing(...)`.
  2. `bootstrap/app.php` limitaba `shouldRenderJsonWhen` a `is('api/*')` — cualquier `fetch()` hacia una ruta de `routes/web.php` (como esta) nunca recibía una respuesta JSON limpia sin importar el header `Accept`, aunque la ruta funcionara. Corregido ampliando la condición a `is('api/*') || expectsJson()`, sin afectar el comportamiento ya probado de `/api/*`.
- **UI**: `App\Livewire\NotificacionesBell` (primer componente Livewire "puro" del proyecto, no envuelto por Filament) registrado por nombre (`Livewire::component('notificaciones-bell', ...)`) y montado en el topbar de **ambos** paneles (`/admin` y `/erp`) vía `renderHook(PanelsRenderHook::TOPBAR_END)` — el super admin también recibe `solicitud.aprobada`/`rechazada`. Dropdown con `<x-filament::dropdown>` (verificado contra el código real de Filament v5, mismo patrón que `tenant-menu.blade.php`), badge de no leídas, "Marcar leída" por fila. En el marketplace: sección "Notificaciones" en `marketplace/dashboard/index.blade.php` (Alpine `notificacionesPanel()`, mismo patrón `fetch()`+CSRF que `favoritoToggle()`/`resenaForm()` de `006`).
- **Extensión menor no listada en `plan.md`**: `Cliente::nombreCompleto()` (accessor `Attribute::make()`) — la lógica "razón social si es jurídica, nombre+apellido si es natural" ya estaba duplicada 3 veces como método privado en `VehiculoResource`/`OrdenTrabajoResource`/`NotaVentaResource`; se centralizó en el modelo para que `NotaEmitidaNotification` no la duplicara una cuarta vez. Los 3 Resources existentes no se tocaron (fuera de alcance de esta sesión).

**40 tests Pest nuevos** en `tests/Feature/Notificaciones/`: `NotificacionTest.php` (CHECK de destinatario único en ambas direcciones, `scopeNoLeidas`, `marcarLeida()` idempotente), `AccionesNotificacionesTest.php` (`CrearNotificacionAction`/`MarcarNotificacionLeidaAction`, ambos guards, ajeno rechazado), `EndpointMarcarLeidaTest.php` (guest 401, ambos guards marcan la propia, ajeno rechazado con 422), `TriggersEventosTest.php` (stock.bajo, pago.registrado, resena.nueva incluyendo el caso "no duplica en edición" y "no dispara en moderación"), `TriggersOrdenYNotaTest.php` (orden.asignada en creación y en edición estándar, sin destinatario si el empleado no tiene acceso, orden.cambio_estado incluida la anulación, nota.emitida), `TriggersSolicitudYUsuarioTest.php`, `PasswordExpiradaCommandTest.php` (ventana de 7 días, no duplica en corridas repetidas, ignora usuario inactivo). **521/521 tests verdes en total** (481 previos + 40 nuevos), `laravel/pint --dirty` sin pendientes, `npm run build` sin errores.
- Migración corrida y verificada contra la BD de desarrollo real. Smoke test con `php artisan serve` + `curl`: `/erp`/`/dashboard` sin sesión → 302 (no 500); `POST /notificaciones/1/marcar-leida` sin sesión → 419 (CSRF, comportamiento esperado de un POST real sin token — no 500). Verificación visual en navegador real no se hizo (`claude-in-chrome` no disponible en esta sesión, mismo motivo estructural de sesiones previas) — cobertura por los tests HTTP reales del endpoint.
- **Efecto colateral real detectado por la suite completa, no por lectura de código**: correr la suite nueva en paralelo con la suite completa (dos procesos `php artisan test` contra la misma `taller_test`) produjo 4 fallos falsos por colisión de `RefreshDatabase` entre procesos (`no existe la relación «talleres»`) — no es un bug de código, es un artefacto de ejecución concurrente contra una sola BD de test; confirmado corriendo la suite completa sola después (521/521 verdes). Documentado acá para que la próxima sesión no repita el susto: no correr dos `php artisan test` a la vez contra `taller_test`.

### Qué falta de `014` (fuera de alcance a propósito)

- Email/SMS/WhatsApp: explícitamente fuera de alcance del MVP en el propio `spec.md`.
- Preferencias de notificación configurables por usuario: explícitamente fuera de alcance del MVP.
- Verificación visual en navegador real: no se hizo (ver arriba).

## Qué se hizo el 2026-07-28 (decimoquinta sesión): 013-pagos completo (481/481 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, sigue a `012-notas-venta`). `013-pagos/spec.md` estaba en `status: draft`; los criterios de aceptación eran suficientes y consistentes con `plan.md`/`tasks.md`, así que se implementó directamente y se pasó a `status: implemented`. Feature pequeña pero cierra la brecha diferida más veces mencionada en `011`/`012`: hasta ahora toda nota de venta quedaba congelada en `EMITIDA`.

**Backend:**
- **2 migraciones**: `metodos_pago` (catálogo global sin `taller_id`, `nombre`/`slug` únicos, `activo` DEFAULT TRUE — mismo patrón que `unidades_medida` de `010`, se desactiva pero nunca se borra), `pagos` (`nota_venta_id`/`metodo_pago_id` FK NOT NULL, `usuario_sistema_id` FK NULL, `fecha_pago` TIMESTAMPTZ DEFAULT NOW(), `monto` NUMERIC(12,2) CHECK `> 0`, `referencia`/`observacion` NULL, `estado` CHECK `IN ('CONFIRMADO','ANULADO')`, **sin** `taller_id` propio — el aislamiento lo garantiza `nota_venta_id`, mismo criterio que `notas_venta_lineas` de `012` —, **sin soft delete**: un pago nunca se borra físicamente, se anula cambiando `estado`, por eso sí lleva `updated_at` a diferencia de un historial append-only real).
- **Modelos** `MetodoPago` (`scopeActivos()`, sin `BelongsToTaller`: catálogo global) y `Pago` (`belongsTo` a `NotaVenta`/`MetodoPago`/`UsuarioSistema`, sin `BelongsToTaller`). Se crean/anulan únicamente vía las dos Actions de `app/Actions/Pagos/`, nunca `::create()` directo desde Filament.
- **`NotaVenta::recalcularTotales()` (de `012`) extendido**: ahora `monto_pagado` ya no es un valor que simplemente se "respeta" — se recalcula siempre como la suma de `pagos()->where('estado','CONFIRMADO')->sum('monto')` (nueva relación `NotaVenta::pagos()`), y el método deriva también `estado` (`PAGADA` si `saldo=0` y `total>0`; `PENDIENTE` si `monto_pagado>0`; `EMITIDA` si `monto_pagado=0`), salvo que la nota ya esté `ANULADA` (guard explícito: `recalcularTotales()` retorna sin tocar nada — 013-spec.md: "una nota anulada no se recalcula ante nuevos pagos"). **Efecto colateral real sobre un test de `012`**: `NotaVentaTest.php` tenía un test que fijaba `monto_pagado` a mano vía el factory (válido cuando `013` no existía, documentado explícitamente así en el comentario del método) — se corrigió para crear un `Pago` real `CONFIRMADO` (y uno `ANULADO` para verificar que se excluye de la suma), mismo patrón de "una feature nueva obliga a tocar un test de una feature anterior" ya documentado en la sesión de `011`.
- **2 Actions en `app/Actions/Pagos/`**: `RegistrarPagoAction` (`lockForUpdate` sobre la fila de la nota — constitution.md §3.6 —, rechaza nota `ANULADA`, método de pago inactivo, monto ≤ 0 o mayor al saldo pendiente; inserta el pago `CONFIRMADO`, llama a `recalcularTotales()` y dispara `PagoRegistrado`), `AnularPagoAction` (`lockForUpdate` sobre nota y pago, rechaza anular un pago que no esté `CONFIRMADO`, cambia el pago a `ANULADO`, recalcula la nota — que vuelve a `PENDIENTE` o `EMITIDA` según corresponda sin lógica adicional, ya la resuelve `recalcularTotales()` — y audita vía `activity()`, mismo patrón que `AnularNotaVentaAction` de `012` ya que `013-plan.md` tampoco define una tabla de historial dedicada). `RegistrarPagoAction` no audita (el registro de pago no es un criterio de auditoría explícito de `013-spec.md`, a diferencia de la anulación).
- **Evento `PagoRegistrado`** disparado al confirmar un pago, **sin listener todavía** — es el trigger `pago.registrado` que `014-notificaciones/plan.md` asigna explícitamente a `013-pagos` ("Cada feature implementa su propio listener/observer" para notificaciones, `014` solo define la tabla). Mismo patrón exacto que `StockBajoDetectado` de `010`: el evento existe y se dispara antes de que exista su consumidor real.
- **`MetodoPagoSeeder`** (efectivo/tarjeta/QR/transferencia, `updateOrCreate` por `slug`, idempotente), registrado en `DatabaseSeeder`. **Decisión deliberada de no agregar un `MetodoPagoResource`** (a diferencia de `UnidadMedidaResource` en `010`): ahí la extensión era necesaria porque sin una pantalla no había ningún dato que seleccionar; aquí el seeder ya deja los 4 métodos usables de inmediato, así que un Resource de administración sería scope-creep no pedido por `plan.md`/`tasks.md`.

**Filament (`/erp`):**
- **`PagosRelationManager`** agregado a `NotaVentaResource::getRelations()` (pestaña nueva junto a `LineasRelationManager`). Modal "Registrar Pago" (`CreateAction` con `using()`+`Halt`, mismo patrón que `RepuestosRelationManager`/`NotasRelationManager` de `011`): método de pago activo, monto con `minValue(0.01)` y `maxValue()` = saldo pendiente en vivo (validación cliente, la Action valida en servidor), helper text con el saldo actual (inyección de `Livewire\Component $livewire` en el closure, verificado contra la documentación real de Filament v5 con Context7 antes de usarlo — primera vez que este proyecto inyecta `$livewire` en un closure de formulario). Acción de fila "Anular pago". **Gateo explícito por permiso** (`pagos.registrar`/`pagos.anular`) en el propio RelationManager — a diferencia de otros RelationManagers del proyecto (que heredan el permiso del recurso dueño), porque son permisos independientes del catálogo de `002`, distintos de `notas.editar`.
- Permisos usados (`pagos.ver/registrar/anular`) ya estaban en el catálogo de `002` desde el principio.

**23 tests Pest nuevos** en `tests/Feature/Pagos/`: `PagoTest.php` (CHECK `monto > 0`, CHECK de `estado`, `MetodoPago` unique nombre + `scopeActivos`, `MetodoPagoSeeder` idempotente crea los 4 métodos, un pago no tiene `BelongsToTaller` propio — el aislamiento es vía la nota padre), `RegistrarPagoActionTest.php` (monto ≤ 0, método inactivo, pago que supera el saldo, nota anulada rechaza pagos, pago parcial deja `PENDIENTE`, pago total deja `PAGADA`, varios pagos parciales sin superar el total, dispara `PagoRegistrado`, guarda el usuario que registró), `AnularPagoActionTest.php` (rechaza anular un pago ya `ANULADO`, `PAGADA`→`EMITIDA` al anular el único pago, `PAGADA`→`PENDIENTE` al anular uno de dos pagos parciales, auditoría real vía `Activity::where(...)`), `PagosRelationManagerTest.php` (Livewire real: registra un pago vía la tabla y recalcula la nota, usuario sin `pagos.registrar` no ve la acción de crear, anula un pago vía la tabla, usuario sin `pagos.anular` no ve la acción de anular). **1 test de `012` actualizado** (`NotaVentaTest.php`, ver arriba). **481/481 tests verdes en total** (458 previos + 23 nuevos), `laravel/pint --dirty` sin pendientes.
- Migraciones corridas y verificadas contra `taller_test` **y contra la BD de desarrollo real** (incluido `php artisan db:seed --class=MetodoPagoSeeder`). Verificado con `php artisan serve` + `curl`: `/erp/notas-venta` y `/erp/notas-venta/1/edit` sin sesión → 302 (no 500). Verificación visual en navegador real no se hizo (`claude-in-chrome` no disponible en esta sesión, mismo motivo estructural de sesiones previas) — cobertura por los 4 tests Livewire reales sobre `PagosRelationManager`.

### Qué falta de `013` (fuera de alcance a propósito)

- **Pagos en línea / pasarelas de pago**: fuera de alcance del MVP según el propio `spec.md`.
- **`MetodoPagoResource`** (gestión de catálogo desde el ERP): no implementado a propósito, ver arriba — el seeder ya deja datos usables; agregarlo sería extender más allá de `plan.md`/`tasks.md`.
- Verificación visual en navegador real: no se hizo (ver arriba).

## Qué se hizo el 2026-07-28 (decimocuarta sesión): 012-notas-venta completo (458/458 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, sigue a `011-ordenes-trabajo`). `012-notas-venta/spec.md` estaba en `status: draft`; los criterios de aceptación eran suficientes y consistentes con `plan.md`/`tasks.md` (incluida la única "decisión pendiente" que `tasks.md` ya resolvía explícitamente: "múltiples notas activas por orden, permisiva"), así que se implementó directamente y se pasó a `status: implemented`.

**Backend:**
- **2 migraciones**: `notas_venta` (`taller_id`/`codigo` UNIQUE por taller, `cliente_id`/`orden_trabajo_id`/`usuario_sistema_id` FK NULL, `estado` con CHECK de las 4 fases, `subtotal`/`descuento`/`total`/`monto_pagado`/`saldo` NUMERIC(12,2) con CHECK de cada fórmula — `total >= 0`, `descuento <= subtotal` y `monto_pagado <= total` **no están listados literalmente en `012-plan.md`** pero sí son criterios explícitos de `012-spec.md`, mismo criterio de lectura ya aplicado en `011` para `total >= 0` —, soft delete), `notas_venta_lineas` (`servicio_catalogo_id`/`repuesto_id` FK NULL con CHECK de mutua exclusión —nunca ambos no-nulos, sí ambos nulos para línea personalizada—, `descripcion` NOT NULL, `cantidad`/`precio_unitario`/`descuento`/`subtotal` con los mismos CHECK de fórmula que `011`, **sin columna `estado`**: a diferencia de las líneas de orden, una línea de nota no tiene máquina de estados propia en el MVP — la nota completa se anula de una vez).
- **Modelos** `NotaVenta` (`BelongsToTaller`, `SoftDeletes`, relaciones a `cliente`/`ordenTrabajo`/`usuarioSistema`/`lineas`) con **`recalcularTotales(?float $descuento = null)`**: suma todas las líneas (no hay estado de línea que filtrar), valida el descuento contra el subtotal, y recalcula `saldo = total - monto_pagado` contra el `monto_pagado` actual — preparado para que `013-pagos` (que no existe todavía) lo invoque tras registrar un pago, mismo patrón de "escritor futuro" que `StockBajoDetectado` de `010` sin listener. `NotaVentaLinea` **sin `BelongsToTaller`** (aislamiento vía `nota_venta_id`, mismo criterio que `OrdenTrabajoServicio`/`OrdenTrabajoRepuesto` de `011`). Relación `OrdenTrabajo::notasVenta()` agregada (consumida por el punto de integración con `011`, ver abajo).
- **Máquina de estados** en `TransicionesEstadoNota` (`EMITIDA`→`PENDIENTE`/`PAGADA`/`ANULADA`, `PENDIENTE`→`PAGADA`/`ANULADA`, `PAGADA` y `ANULADA` finales sin destinos listados). Mismo criterio de lectura literal ya aplicado a `ENTREGADA` en `011`: la nota informal del spec junto a `PAGADA` ("no se recomienda anular salvo error") no se implementó como transición real. En la práctica, aunque la tabla sí lista `PENDIENTE`→`ANULADA` como válida, `AnularNotaVentaAction` la bloquea siempre por la regla independiente "si tiene pagos confirmados, se bloquea" (`PENDIENTE` implica `monto_pagado > 0` por definición) — la única anulación que prospera de verdad es desde `EMITIDA`.
- **3 Actions en `app/Actions/Notas/`**: `CrearNotaVentaDesdeOrdenAction` (exige orden `COMPLETADA`/`ENTREGADA` con al menos una línea activa, copia el snapshot ya congelado de cada línea de servicio/repuesto tal cual — sin volver a tocar el catálogo ni el inventario, porque `012-spec.md` es explícito en que una nota desde orden nunca genera una segunda salida), `CrearNotaVentaDirectaAction` (líneas manuales de servicio/repuesto/personalizada con snapshot de precio desde catálogo — mismo criterio que `AgregarLineaServicioAction`/`AgregarLineaRepuestoAction` de `011` —, cada línea de repuesto descuenta stock real de inmediato vía `RegistrarMovimientoInventarioAction`, toda la creación en una sola transacción para que un stock insuficiente en cualquier línea deje la nota completa sin persistir, sin movimiento huérfano), `AnularNotaVentaAction` (transacción única: motivo obligatorio, transición válida, bloqueo si `monto_pagado > 0` con el mensaje literal del spec "Debe reversar los pagos primero", repone stock solo si la nota es venta directa —`orden_trabajo_id` nulo—, auditoría vía `activity()` porque `012-plan.md`, a diferencia de `011`, no define una tabla de historial dedicada para notas). El código se genera con `SequentialCodeGenerator` directamente (formato `NV-YYYY-###`), mismo criterio de reutilización sin envolver en una Action `GenerarCodigoNota` propia ya aplicado en `011` para `GenerarCodigoOrden`.
- **Brecha de `011` resuelta en esta sesión**: `011-ordenes-trabajo/spec.md` exige que anular una orden bloquee si tiene una nota `PENDIENTE`/`PAGADA` y auto-anule una nota `EMITIDA` — quedó documentado como diferido en `011` porque `012` no existía. `AnularOrdenTrabajoAction` ahora consulta `orden->notasVenta()` (no ANULADA): si alguna está `PENDIENTE`/`PAGADA` bloquea con "Resuelva la nota NV-XXX primero — tiene pagos registrados"; si no, auto-anula cada nota `EMITIDA` activa vía `AnularNotaVentaAction` (misma transacción, generalizado a "cada nota activa" porque `012-spec.md` permite varias notas por orden, aunque `011-spec.md` lo redacte en singular).

**Filament (`/erp`):**
- **`NotaVentaResource`**: creación real vía `CrearNotaVentaDesdeOrdenAction`/`CrearNotaVentaDirectaAction` según un campo `origen` (Radio, no es columna del modelo) que alterna entre un `Select` de orden completada/entregada y un `Repeater` de líneas manuales (tipo servicio/repuesto/personalizada, con visibilidad condicional por tipo). **Hallazgo real de Filament v5** verificado en esta sesión: `->visible()` en un componente no lo excluye de la validación (`required()`) — un campo oculto por `origen` distinto igual dispara "campo requerido" si su regla `required()` no está gateada explícitamente por la misma condición; corregido gateando cada `required()` con el mismo `Get` que controla la visibilidad (dentro del `Repeater`, con `../../origen` para llegar al campo del nivel superior). También se evitó `Repeater::minItems(1)` a propósito: sembraba un ítem por defecto en el estado incluso oculto, disparando la misma clase de error — "al menos una línea" ya lo exige la Action con un mensaje de negocio legible. Nada es editable después de crear la nota (sin Action de edición de líneas en `012-tasks.md`): `EditNotaVenta` solo muestra campos deshabilitados + la acción de cabecera "Anular Nota" (mismo patrón que `EditOrdenTrabajo` de `011`).
- **`LineasRelationManager`**: solo lectura (sin `headerActions`/`recordActions`), mismo patrón que `HistorialRelationManager` de `011` — no hay Action para editar una línea después de creada la nota.
- Permisos usados (`notas.ver/crear/editar/anular/eliminar`) ya estaban en el catálogo de `002` desde el principio.

**48 tests Pest nuevos** en `tests/Feature/Notas/`: `NotaVentaTest.php` (aislamiento por taller, auto-fill de `taller_id`, todos los CHECK de ambas migraciones incluida la mutua exclusión servicio/repuesto, soft delete, `recalcularTotales()` unitario incluyendo el respeto a `monto_pagado`), `CrearNotaVentaDesdeOrdenActionTest.php` (copia de líneas activas, no descuenta inventario dos veces, rechaza orden no completada, rechaza sin líneas, múltiples notas por orden), `CrearNotaVentaDirectaActionTest.php` (servicio+repuesto+personalizada en una misma nota, descuenta stock solo del repuesto, mutua exclusión, descuento vs. bruto, **stock insuficiente no deja nota ni movimiento huérfano** — verificado con dos líneas donde la primera sí tiene stock y la transacción completa revierte igual), `AnularNotaVentaActionTest.php` (motivo obligatorio, `PAGADA` sin transiciones, bloqueo por pagos con el mensaje exacto del spec, reposición solo en venta directa, sin reposición si viene de una orden), `ResourcesAutorizacionTest.php`, `CreacionFormularioTest.php` (Livewire, incluye el caso "orden no completada" rechazado por la Action). **6 tests agregados a `tests/Feature/Ordenes/AnularOrdenTrabajoActionTest.php`** para la integración con `011` (bloqueo por nota `PENDIENTE`/`PAGADA`, auto-anulación de una y de varias notas `EMITIDA`, sin reposición de inventario para una nota vinculada a la orden). **458/458 tests verdes en total** (410 previos + 48 nuevos), `laravel/pint --dirty` sin pendientes.
- Migraciones corridas y verificadas contra `taller_test` **y contra la BD de desarrollo real**. Verificado con `php artisan serve` + `curl`: `/erp/notas-venta` y `/erp/notas-venta/create` sin sesión → 302 (no 500). Verificación visual en navegador real no se hizo (`claude-in-chrome` no disponible en esta sesión, mismo motivo estructural de sesiones previas) — cobertura por los tests Livewire reales sobre `CreateNotaVenta`.

### Qué falta de `012` (fuera de alcance a propósito)

- **Transiciones reales a `PENDIENTE`/`PAGADA`**: en el MVP de esta sesión toda nota se crea y permanece `EMITIDA` (`monto_pagado` siempre 0) porque `013-pagos` no existe todavía — es quien debe escribir `monto_pagado` y disparar esas transiciones. `recalcularTotales()` ya deja `saldo` consistente contra `monto_pagado` para cuando esa feature exista.
- **"Registrar Pago" en la UI** (`012-plan.md` lo menciona como botón futuro): no implementado, depende de `013-pagos`.
- Notas de crédito o devoluciones, facturación fiscal: fuera de alcance del MVP según el propio `spec.md`.
- Verificación visual en navegador real: no se hizo (ver arriba).

## Qué se hizo el 2026-07-28 (decimotercera sesión): 011-ordenes-trabajo completo (410/410 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, sigue a `010-inventario-repuestos`). `011-ordenes-trabajo/spec.md` estaba en `status: draft`; los criterios de aceptación eran suficientes y consistentes con `plan.md`/`tasks.md` salvo por una brecha estructural documentada abajo, así que se implementó directamente y se pasó a `status: implemented` (mismo criterio que `AGENTS.md` permite sin preguntar). Es, con diferencia, la feature más grande implementada hasta ahora: documento central del ERP con máquina de estados de orden, máquina de estados de dos tipos de línea, totales transaccionales, y la primera integración real cruzando features (`010-inventario-repuestos`).

**Backend:**
- **6 migraciones**: `ordenes_trabajo` (`taller_id`/`cliente_id`/`vehiculo_id` FK NOT NULL, `empleado_asignado_id`/`creado_por_usuario_sistema_id` FK NULL, `estado`/`prioridad` con CHECK, `subtotal_servicios`/`subtotal_repuestos`/`descuento`/`total` NUMERIC(12,2) con CHECK `total = subtotal_servicios + subtotal_repuestos - descuento` y CHECK `total >= 0` — este último no estaba explícito en `plan.md` pero se dedujo directamente de "total no puede ser negativo" del spec —, `UNIQUE(taller_id, codigo)`, soft delete), `ordenes_trabajo_servicios`/`ordenes_trabajo_repuestos` (misma estructura, `cantidad` SMALLINT vs. NUMERIC(12,3), CHECK `subtotal = cantidad*precio_unitario-descuento` + CHECK `subtotal >= 0` añadido para reforzar a nivel de BD la regla "el descuento de línea no puede superar el bruto"), `ordenes_trabajo_estados_historial` (append-only, sin `updated_at`), `ordenes_trabajo_notas` (`INTERNA`/`PUBLICA`). **Migración adicional** `add_orden_trabajo_repuesto_foreign_to_inventario_movimientos_table`: agrega la FK real que `010` dejó pendiente a propósito (documentado en su propia migración) sobre `inventario_movimientos.orden_trabajo_repuesto_id`, ahora que `ordenes_trabajo_repuestos` existe.
- **Modelos** `OrdenTrabajo` (`BelongsToTaller`, `SoftDeletes`, relaciones a `cliente`/`vehiculo`/`empleadoAsignado`/`creadoPor`/`lineasServicios`/`lineasRepuestos`/`historial`/`notas`) con el método central **`recalcularTotales(?float $descuento = null)`**: recalcula `subtotal_servicios`/`subtotal_repuestos` sumando solo líneas no `ANULADO`, valida el descuento (no negativo, no mayor a la suma de subtotales) y guarda — invocado dentro de la misma transacción que cualquier alta/baja/cambio de estado de línea (constitution.md §3.7). `OrdenTrabajoServicio`/`OrdenTrabajoRepuesto`/`OrdenTrabajoHistorialEstado`/`OrdenTrabajoNota` **sin `BelongsToTaller`** (a diferencia de todas las entidades de features anteriores): el aislamiento por taller ya lo garantiza la orden padre vía `orden_trabajo_id`, mismo criterio que evitó duplicar `taller_id` en el pivot `repuestos_proveedores` de `010`. Relaciones inversas agregadas a `Cliente::ordenesTrabajo()`, `Vehiculo::ordenesTrabajo()`, `Empleado::ordenesAsignadas()` (primer consumidor real de los `scopeActivos()` que esas tres features dejaron preparados desde `007`/`008`).
- **Máquina de estados de orden** en `TransicionesEstadoOrden` (tabla única compartida por `CambiarEstadoOrdenAction` y `AnularOrdenTrabajoAction`, evita duplicar la tabla del spec en dos sitios). **Decisión de lectura literal del spec**: la fila de `ENTREGADA` en la tabla de transiciones no lista ningún destino (a diferencia de `COMPLETADA`, que sí lista `ANULADA` explícitamente); la nota informal junto a `ENTREGADA` ("no se recomienda anular salvo error") no se implementó como una transición real porque el propio spec dice inmediatamente después "cualquier transición no listada se rechaza" — se aplicó esa regla literalmente incluso a este caso ambiguo, en vez de asumir que la nota implica una transición permitida. Documentado extensamente en el docblock de `TransicionesEstadoOrden` para que quede claro que fue una decisión deliberada, no un olvido.
- **7 Actions en `app/Actions/Ordenes/`**: `CrearOrdenTrabajoAction` (valida cliente/vehículo/empleado dentro del taller activo, genera código con **`SequentialCodeGenerator`** — clase genérica ya construida en `017-infraestructura-sistema` pero sin ningún consumidor real hasta ahora; se usó directamente en vez de envolverla en una Action `GenerarCodigoOrden` propia, satisfaciendo esa tarea de `tasks.md` por reutilización en vez de duplicación —, inserta el historial inicial), `CambiarEstadoOrdenAction` (transiciones normales, rechaza explícitamente `ANULADA` remitiendo a la Action dedicada), `AnularOrdenTrabajoAction` (transacción única: repone stock de líneas `ENTREGADO` vía `RegistrarMovimientoInventarioAction` de `010` con tipo `AJUSTE_POSITIVO`, marca todas las líneas no anuladas como `ANULADO`, **NO recalcula totales** — los deja congelados como registro histórico de lo que valía la orden, evitando además un conflicto real con el CHECK `descuento <= subtotal_servicios+subtotal_repuestos` si se hubiera recalculado a cero con un descuento previo distinto de cero —, motivo obligatorio, historial), `AgregarLineaServicioAction`/`AgregarLineaRepuestoAction` (snapshot de `precio_unitario` desde el catálogo/repuesto, validación de descuento vs. bruto, recalcula totales), `CambiarEstadoLineaServicioAction`/`CambiarEstadoLineaRepuestoAction` (máquinas de estado de línea; la de repuesto integra `RegistrarMovimientoInventarioAction` con `ordenTrabajoRepuestoId` = id de la línea para la protección de doble salida que `010` ya tenía lista y testeada sin consumidor hasta hoy), `AgregarNotaOrdenAction`, `ActualizarDescuentoOrdenAction` (no estaba en `tasks.md` explícitamente pero es necesaria para el criterio "descuento no puede superar la suma de subtotales" a nivel de orden, no solo de línea — delega en `recalcularTotales()`).
- **Extensión no trivial deducida del spec, no inventada**: anular una línea de repuesto individual que ya estaba `ENTREGADO` (fuera de una anulación completa de orden) también repone stock, aplicando el mismo principio de "no dejar stock inconsistente" que el spec sí exige explícitamente para la anulación de toda la orden. No es un criterio de aceptación literal de `011-spec.md`, pero es la misma regla de negocio ya declarada aplicada de forma consistente a un caso más granular — documentado como tal en el docblock de `CambiarEstadoLineaRepuestoAction`.
- **Brecha documentada a propósito (forward-dependency real, no un olvido)**: el spec de anulación exige bloquear si la orden tiene una nota de venta `PENDIENTE`/`PAGADA` y auto-anular una nota `EMITIDA` — `012-notas-venta` no existe todavía (no es dependencia de `011` en su `depends_on`). `AnularOrdenTrabajoAction` no implementa ese paso: no hay tabla `notas_venta` que consultar, así que ninguna orden puede tener una nota de venta real hoy y omitirlo no cambia ningún comportamiento observable actual. Extensamente documentado en el docblock de la Action para que `012` lo agregue cuando exista — mismo patrón de brecha diferida que `orden_trabajo_id` sin FK en `resenas` (`006`) o `orden_trabajo_repuesto_id` sin FK en `inventario_movimientos` (`010`, resuelto en esta misma sesión).

**Filament (`/erp`):**
- **`OrdenTrabajoResource`**: creación real vía `CrearOrdenTrabajoAction` (no `OrdenTrabajo::create()` directo, mismo patrón `handleRecordCreation()` + `Halt::rollBackDatabaseTransaction()` que `CreateEmpleado` de `008`). Edición de campos simples (empleado/prioridad/fechas/observaciones) sí usa el flujo estándar de Filament — sin Action dedicada, porque `canEdit()` ya bloquea toda edición de una orden `ANULADA` a nivel de autorización, no hace falta duplicar esa regla en una Action. El campo `estado` **no es editable en el formulario**: cambio de estado y anulación son acciones de cabecera dedicadas en `EditOrdenTrabajo` (`Cambiar estado` con un `Select` cuyas opciones se calculan en vivo desde `TransicionesEstadoOrden::MAPA[$estadoActual]`, y `Anular orden` con motivo obligatorio), para que el dropdown nunca ofrezca una transición inválida.
- **4 `RelationManagers`** (aparecen automáticamente como pestañas en `EditOrdenTrabajo`, sin necesidad de código extra — mismo comportamiento por defecto ya usado sin pestañas múltiples desde `010`): `ServiciosRelationManager`/`RepuestosRelationManager` (el `CreateAction` de la tabla no hace `::create()` directo, se intercepta con `->using()` + `Halt` para pasar por `AgregarLinea*Action`; acciones de fila "Marcar realizado"/"Entregar"/"Anular línea" llaman a las Actions de cambio de estado de línea), `NotasRelationManager` (mismo patrón `using()`+`Halt`, sin `EditAction`/`DeleteAction` — una nota no se corrige después), `HistorialRelationManager` (100% solo lectura, sin `headerActions`/`recordActions`, mismo patrón que `MovimientoInventarioResource` de `010`). **Toast específico de stock insuficiente** (`011-plan.md`: "Stock insuficiente para [Repuesto]. Disponible: X"): `RepuestosRelationManager` no muestra el mensaje genérico de `RegistrarMovimientoInventarioAction`, lo detecta por substring y reconstruye un mensaje con el nombre real del repuesto y el stock disponible actual — verificado con test real (`Str::contains`, no un catálogo de tipos de excepción nuevo, para no tocar la Action de `010` ya estable).
- **Simplificación de diseño consciente, comunicada aquí**: `011-plan.md` pide una "vista Kanban/lista por estado". No existe ningún paquete de Kanban instalado en el proyecto y construir uno a mano (drag-and-drop real) no es requisito de ningún criterio de aceptación medible de `011-spec.md` — se implementó como **lista con una pestaña por estado** (`ListOrdenesTrabajo::getTabs()`, un tab "Todas" + uno por cada uno de los 8 estados), que cubre la intención funcional ("ver órdenes agrupadas por estado") sin la complejidad de un Kanban interactivo. Documentado explícitamente en el docblock de `ListOrdenesTrabajo` como decisión deliberada, no como una tarea pendiente.
- Permisos usados (`ordenes.ver/crear/editar/anular/eliminar`) ya estaban en el catálogo de `002` desde el principio — nótese que `ordenes.anular` es el "permiso especial" que el spec exige para anular, distinto de `ordenes.editar`.

**67 tests Pest nuevos** en `tests/Feature/Ordenes/`: `OrdenTrabajoTest.php` (aislamiento por taller, auto-fill de `taller_id`, todos los CHECK de la migración incluyendo la fórmula de `total`, soft delete, `recalcularTotales()` unitario), `CrearOrdenTrabajoActionTest.php`, `CambiarEstadoOrdenActionTest.php` (incluye el caso literal de `ENTREGADA` sin transiciones), `AnularOrdenTrabajoActionTest.php` (motivo obligatorio, reposición real de stock, totales congelados no recalculados), `LineasServicioActionsTest.php`/`LineasRepuestoActionsTest.php` (snapshot de precio, descuento vs. bruto, **stock insuficiente dejando la línea intacta y sin movimiento huérfano**, doble entrega imposible, reposición de stock al anular una línea `ENTREGADO`), `NotasYDescuentoOrdenActionsTest.php`, `ResourcesAutorizacionTest.php`, `CreacionFormularioTest.php` (Livewire, incluye el caso "vehículo de otro cliente" rechazado por la Action y no solo por el formulario), **`RelationManagersTest.php`** (Livewire directo contra los 4 `RelationManagers` vía `callTableAction()`/`assertNotified()` — primera vez en el proyecto que se testean RelationManagers de esta forma, no solo la carga de la página que los contiene; incluye el toast exacto de stock insuficiente con nombre y disponible). **410/410 tests verdes en total** (343 previos + 67 nuevos), `laravel/pint --dirty` sin pendientes.
- **Efecto colateral real detectado por la suite completa, no por lectura de código**: al agregar la FK real de `orden_trabajo_repuesto_id`, dos tests preexistentes de `tests/Feature/Inventario/RegistrarMovimientoInventarioActionTest.php` (`010`) que usaban enteros arbitrarios (999, 1, 2) como `ordenTrabajoRepuestoId` — válido cuando la columna no tenía FK — empezaron a fallar con `SQLSTATE[23503]` (violación de llave foránea). Corregido agregando un helper `lineaRepuestoParaMovimiento()` que crea una fila real de `ordenes_trabajo_repuestos` en esos dos tests. Es la primera vez en el proyecto que una feature nueva obliga a tocar tests de una feature anterior — se dejó documentado en el propio test file.
- Migraciones corridas y verificadas contra `taller_test` **y contra la BD de desarrollo real** (`php artisan migrate`, primera vez que se corre ahí desde `009` — las migraciones de `010` tampoco se habían corrido contra la BD real hasta esta sesión). Verificado con `php artisan serve` + `curl`: `/erp/ordenes-trabajo` y `/erp/ordenes-trabajo/create` sin sesión → 302 (no 500). Verificación visual en navegador real no se hizo (`claude-in-chrome` no disponible en esta sesión, mismo motivo estructural de sesiones previas) — cobertura por los tests Livewire reales sobre `CreateOrdenTrabajo` y los 4 `RelationManagers`.

### Qué falta de `011` (fuera de alcance a propósito)

- ~~Interacción real con nota de venta al anular (bloqueo si `PENDIENTE`/`PAGADA`, auto-anulación si `EMITIDA`): no se puede implementar todavía porque `012-notas-venta` no existe.~~ **Resuelto en la decimocuarta sesión** (`012-notas-venta` ya existe): `AnularOrdenTrabajoAction` consulta `orden->notasVenta()` y bloquea/auto-anula según corresponda.
- **Kanban con drag-and-drop**: se implementó como lista con pestañas por estado (ver arriba). Si el usuario pide explícitamente una interacción de arrastrar, sería una feature de UI adicional, no un criterio de aceptación de `011-spec.md`.
- Verificación visual en navegador real: no se hizo (ver arriba).
- **Código de `011` commiteado y pusheado en `dbe231e` en `origin/specs/planificacion`.**
- **Código de `012` commiteado y pusheado en `d5d8f14` en `origin/specs/planificacion`.**

## Qué se hizo el 2026-07-27 (duodécima sesión): 010-inventario-repuestos completo (343/343 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, sigue a `009-catalogo-servicios`). El usuario pidió explícitamente no commitear nada en esta sesión — **todo el código de esta sesión queda en el working tree, sin `git add`/`commit`**. `010-inventario-repuestos/spec.md` estaba en `status: draft`; los criterios de aceptación eran suficientes y consistentes con `plan.md`/`tasks.md`, así que se implementó directamente y se pasó a `status: implemented` (mismo criterio que `AGENTS.md` permite sin preguntar).

**Backend:**
- **5 migraciones**: `unidades_medida` (catálogo global, sin `taller_id`, `nombre`/`simbolo` únicos), `repuestos` (`taller_id` FK, `UNIQUE(taller_id, codigo)`, índice único parcial `UNIQUE(taller_id, codigo_barras) WHERE codigo_barras IS NOT NULL`, CHECK `stock_actual >= 0`/`precio_costo >= 0`/`precio_venta >= 0`, soft delete), `proveedores` (`taller_id` FK, índice único parcial `UNIQUE(taller_id, nit) WHERE nit IS NOT NULL`, soft delete), `repuestos_proveedores` (pivot N:M, PK compuesta sin `id` propio — mismo patrón que `talleres_categorias` de `003` —, `codigo_proveedor`/`precio_referencia`/`tiempo_entrega_dias`/`es_principal`), `inventario_movimientos` (append-only, sin `updated_at`, CHECK de `tipo_movimiento IN (...)`/`cantidad > 0`/`stock_resultante >= 0`, `orden_trabajo_repuesto_id` **sin FK todavía** — `ordenes_trabajo_repuestos` no existe hasta `011-ordenes-trabajo`, documentado en la propia migración, mismo patrón que `orden_trabajo_id` de `resenas` en `006`).
- **Modelos** `UnidadMedida` (global, `scopeActivos()`), `Repuesto`/`Proveedor` (`BelongsToTaller` + `SoftDeletes`, `scopeActivos()`), `InventarioMovimiento` (`BelongsToTaller`, `const UPDATED_AT = null`). **Mutators `Attribute::make()`** en `Repuesto::codigoBarras()` y `Proveedor::nit()` que normalizan `''` a `null` — mismo riesgo real de falso-positivo en el índice único parcial que `Cliente::nitCi()` detectó en `007`, aplicado aquí de forma preventiva (ya conocido, no hubo que redescubrirlo).
- **Bug real encontrado por los tests, no por lectura de código**: el modelo `Proveedor` no declaraba `protected $table`, así que Eloquent pluralizaba "Proveedor" a **"proveedors"** (pluralización en inglés, `+s`), no "proveedores" — a diferencia de `Repuesto`→"repuestos"/`Cliente`→"clientes"/`Empleado`→"empleados", donde la pluralización en inglés coincide por casualidad con el nombre de tabla en español. Los primeros 15 tests de proveedores fallaron con `SQLSTATE[42P01]: no existe la relación «proveedors»`; corregido agregando `protected $table = 'proveedores'` (mismo patrón explícito que ya usaba `ServicioCatalogo`, cuya tabla `servicios_catalogo` tampoco coincide con la pluralización por defecto).
- **Action `RegistrarMovimientoInventarioAction`** (`app/Actions/Inventario/`): único punto de escritura de `inventario_movimientos`/`repuestos.stock_actual` — `lockForUpdate()` sobre el repuesto dentro de `DB::transaction()`, calcula `stock_resultante` según el tipo (`ENTRADA`/`AJUSTE_POSITIVO` suman, `SALIDA`/`AJUSTE_NEGATIVO` restan), rechaza con `BusinessException` si quedaría negativo o si la cantidad no es positiva. Protección real de **doble salida por línea de orden**: si `tipo_movimiento === 'SALIDA'` y se pasa `orden_trabajo_repuesto_id`, rechaza si ya existe una `SALIDA` previa con ese mismo id — verificado con test aunque `ordenes_trabajo_repuestos` no exista todavía (se usa un entero arbitrario en el test, la Action no depende de que la tabla exista porque la columna no tiene FK real).
- **Evento `StockBajoDetectado`** disparado al final de la Action si `stock_resultante <= stock_minimo`. **Sin listener todavía** — el consumidor real es `014-notificaciones`, que no existe (mismo patrón que `ResenaGuardada`/`orden_trabajo_id` de `006`: el evento se dispara antes de que exista quien lo escuche).
- **Form Requests** `RepuestoRequest`/`ProveedorRequest` (`app/Http/Requests/Repuestos/`, `.../Proveedores/`): unicidad de `codigo`/`codigo_barras`/`nit` acotada a `session('taller_activo_id')`, mismo patrón que `ClienteRequest`/`ServicioCatalogoRequest`. `stock_actual` deliberadamente fuera de las reglas — nunca se edita a mano.

**Filament:**
- **`RepuestoResource`** (`/erp`): `stock_actual` se muestra deshabilitado y solo en edición (`disabled()->dehydrated(false)->visibleOn('edit')`), nunca editable desde el formulario. Columna de stock en rojo/negrita si `stock_actual <= stock_minimo`. Acción de tabla **"Ajustar Stock"** (gate `inventario.ajustar`): modal con tipo/cantidad/motivo que llama a `RegistrarMovimientoInventarioAction`, captura `BusinessException` y la muestra como notificación roja (mismo patrón que `UsuarioResource`/`SolicitudTallerResource`).
- **`ProveedorResource`** (`/erp`): CRUD simple. Incluye la primera `RelationManager` de Filament del proyecto (`ProveedorResource\RelationManagers\RepuestosRelationManager`, N:M con pivot `repuestos_proveedores`) — se gestiona desde el proveedor ("ver/editar la lista de precios de un proveedor"), no duplicada en `RepuestoResource`. Verificado contra Context7 (docs reales de Filament v5, primer uso de `AttachAction`/`EditAction` con datos de pivot en este proyecto) antes de escribir el código: `AttachAction::make()->schema(fn ($action) => [$action->getRecordSelect(), ...campos de pivot...])`, `recordSelectOptionsQuery()` para filtrar solo repuestos activos, `EditAction::make()` sin configuración extra porque Filament ya rellena/persiste columnas de pivot automáticamente cuando están en `withPivot()` de la relación (confirmado leyendo `vendor/filament/actions/src/EditAction.php`, no asumido).
- **`MovimientoInventarioResource`** (`/erp`, solo lectura): `canCreate`/`canEdit`/`canDelete` = `false` — los movimientos son append-only, se crean únicamente vía la Action. Filtros por tipo y por repuesto.
- **`UnidadMedidaResource`** (`/admin`, catálogo global): **no estaba en la lista de Resources de `010-plan.md`** (que solo lista Repuesto/Proveedor/Movimiento) — se agregó porque sin ninguna pantalla, `repuestos.unidad_medida_id` no tendría ningún dato real que seleccionar en el ERP. Mismo tipo de extensión ya documentada y aceptada en `008` (otorgar acceso después de crear el empleado). Mismo patrón que `CategoriaResource`: gateado con `esSuperAdmin()` directo, sin permiso dedicado.
- Permisos usados (`repuestos.ver/crear/editar/eliminar`, `inventario.ver/ajustar`, `proveedores.ver/crear/editar/eliminar`) ya estaban en el catálogo de `002` desde el principio — nótese que no existe `inventario.eliminar` (coherente con que los movimientos son append-only, nunca se borran).

**56 tests Pest nuevos** en `tests/Feature/Inventario/`: `RepuestoTest.php`/`ProveedorTest.php` (aislamiento por taller, auto-fill de `taller_id`, UNIQUE simples y parciales, normalización `''`→`null`, CHECKs de precio/stock, soft delete, `scopeActivos`, pivot proveedor↔repuesto con datos reales), `RegistrarMovimientoInventarioActionTest.php` (los 4 tipos de movimiento y su efecto en el signo, rechazo de stock negativo con verificación de que no queda movimiento huérfano, cantidad cero/negativa rechazada, tipo inválido rechazado, doble SALIDA por `orden_trabajo_repuesto_id` imposible, SALIDA de líneas distintas sí permitida, `StockBajoDetectado` se dispara/no se dispara según el umbral, CHECK de `cantidad > 0` a nivel de BD), `RepuestoRequestTest.php`/`ProveedorRequestTest.php` (unicidad de `codigo`/`codigo_barras`/`nit` por taller vía Form Request), `ResourcesAutorizacionTest.php` (permisos independientes + aislamiento por taller + páginas reales para los 3 Resources del ERP), `CreacionFormularioTest.php` (7 tests Livewire: crear repuesto con `stock_actual` en cero, precio negativo rechazado, código duplicado rechazado, segundo repuesto sin `codigo_barras` sin colisión, crear proveedor, NIT duplicado rechazado). **343/343 tests verdes en total** (287 previos + 56 nuevos), `laravel/pint --dirty` sin pendientes sobre los archivos de esta sesión (hay 3 archivos con deuda de pint preexistente de sesiones anteriores — `Login.php`/`AdminPanelProvider.php`/`ErpPanelProvider.php` — no tocados aquí, fuera de alcance).
- Migraciones corridas y verificadas contra `taller_test` (`php artisan migrate --env=testing`). **No se verificó contra la BD de desarrollo real ni con `php artisan serve`+`curl`** en esta sesión (a diferencia de sesiones previas) — el usuario pidió explícitamente no commitear, se priorizó dejar la suite de tests como fuente de verdad. Verificación visual en navegador real tampoco se hizo (mismo motivo estructural de sesiones previas, `claude-in-chrome` no disponible).

### Qué falta de `010` (fuera de alcance a propósito)

- **Consumo automático real** (línea de repuesto de orden pasa a `ENTREGADO` → `SALIDA` automática; anulación después de `ENTREGADO` → `AJUSTE_POSITIVO` opcional): la Action y la protección de doble salida están listas y testeadas, pero el disparador real (cambio de estado de una línea de `ordenes_trabajo_repuestos`) es responsabilidad de `011-ordenes-trabajo`, que no existe todavía.
- **Notificación real de stock bajo**: el evento `StockBajoDetectado` se dispara pero no tiene listener — el consumidor es `014-notificaciones`, no implementada.
- Órdenes de compra a proveedor y múltiples almacenes: fuera de alcance del MVP según el propio `spec.md`.
- Verificación en BD de desarrollo real y en navegador: no se hizo en esta sesión (ver arriba).
- **Código de `010` commiteado y pusheado en `b7e59be` en `origin/specs/planificacion`.**

## Qué se hizo el 2026-07-26 (undécima sesión): 009-catalogo-servicios completo (287/287 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, sigue a `008-empleados-usuarios-erp`). Feature simple, sin bugs de negocio nuevos — mismo patrón exacto que `007-clientes-vehiculos` (una sola entidad con `BelongsToTaller`, sin dependencias de otro modelo del ERP).

**Backend:**
- **Migración `servicios_catalogo`**: `taller_id` FK NOT NULL, `codigo`/`nombre`/`descripcion` (TEXT NULL), `precio_base` NUMERIC(12,2) DEFAULT 0 con CHECK `>= 0`, `duracion_minutos` INTEGER NULL, `activo` BOOLEAN DEFAULT TRUE, soft delete, `UNIQUE(taller_id, codigo)`.
- **Modelo `ServicioCatalogo`** (`BelongsToTaller`, `SoftDeletes`, `scopeActivos()` mismo patrón que `Cliente`/`Vehiculo` — consumidor real recién en `011`/`012`), cast `precio_base` como `decimal:2`.
- **`ServicioCatalogoRequest`** (`app/Http/Requests/Servicios/`): unicidad de `codigo` acotada a `session('taller_activo_id')`, `precio_base` con `numeric|min:0`. Sin controlador/ruta propia, mismo patrón que `ClienteRequest`/`VehiculoRequest` de `007` — Filament usa reglas equivalentes en su Schema.
- **Filament `ServicioResource`** (`/erp`): formulario simple (código, nombre, descripción, precio base con prefijo "Bs" y `minValue(0)`, duración en minutos, toggle activo), tabla con columna de precio formateada `->money('BOB')`. Permisos `servicios.ver/crear/editar/eliminar` ya estaban en el catálogo de `002` desde el principio.
- **19 tests Pest nuevos** en `tests/Feature/Servicios/`: `ServicioCatalogoTest.php` (aislamiento por taller, auto-fill de `taller_id`, `UNIQUE(taller_id, codigo)`, CHECK `precio_base >= 0` — verificado que `TextInput::minValue()` de Filament sí agrega una regla de validación real `min:{valor}`, no solo el atributo HTML5, confirmado leyendo `vendor/filament/forms/src/Components/TextInput.php` antes de confiar en el test Livewire —, soft delete, `scopeActivos()`), `ServicioCatalogoRequestTest.php` (unicidad de código por taller vía Form Request, precio negativo rechazado), `ResourcesAutorizacionTest.php` (permisos independientes + aislamiento por taller + páginas reales), `CreacionFormularioTest.php` (3 tests Livewire: crear servicio, precio negativo rechazado, código duplicado rechazado). **287/287 tests verdes en total** (267 previos + 19 nuevos — el conteo previo real de la décima sesión era 267, no 268, ver nota de esa sesión), `laravel/pint` sin pendientes (auto-fix de orden de imports en `ServicioResource.php`, verificado que la suite siguió verde después).
- Migración corrida contra la BD de desarrollo real (no solo `taller_test`). Verificado con `php artisan serve` + `curl`: `/erp/servicios` y `/erp/servicios/create` sin sesión → 302 (no 500).

### Qué falta de `009` (fuera de alcance a propósito)

- **Snapshot real de `precio_unitario` en líneas de orden/nota** (criterio de aceptación del spec: "cambio de precio_base no altera líneas históricas"): no se puede verificar con una línea real todavía porque `ordenes_trabajo`/`notas_venta` no existen (`011`/`012`, specs posteriores en el orden del proyecto). El test agregado solo confirma que `precio_base` es un valor mutable simple sin ningún mecanismo de propagación retroactiva — la garantía real depende de que la línea guarde su propio `precio_unitario` al crearse, responsabilidad de esas features cuando lleguen.
- Verificación visual en navegador real: no se hizo (`claude-in-chrome` no disponible en esta sesión, mismo motivo que sesiones previas) — cobertura por los 3 tests Livewire que ejercitan el guardado real del formulario.
- **Código de `009` commiteado y pusheado en `76a1e7a` en `origin/specs/planificacion`.**

## Hotfix 2026-07-26: login de Filament usa `username` en vez de `email`

**Problema:** La tabla `usuarios_sistema` (001-identidad-autenticacion) nunca tuvo columna `email` — el email vive en `identidades` (relación 1:1). Filament por defecto usa `email` en su formulario de login, causando `SQL error: column email does not exist` al intentar iniciar sesión en `/admin` o `/erp`.

**Solución:** Se creó `app/Filament/Auth/Pages/Login.php` que extiende `Filament\Auth\Pages\Login` y sobrescribe:
- `getEmailFormComponent()` → campo `username` con label "Usuario"
- `getCredentialsFromFormData()` → envía `['username' => ..., 'password' => ...]`
- `throwFailureValidationException()` → error sobre `data.username`

Registrado en ambos `PanelProvider` via `->login(\App\Filament\Auth\Pages\Login::class)`.

**Solo afecta a usuarios sistema** (super admin y ERP). Los usuarios marketplace solo usan Google OAuth. No hay cambio en vistas Blade, el HTML lo genera Filament.

## Hotfix 2026-07-26: `CheckSessionExpiration` rompía login con `->lt()` sobre string

**Problema:** `CheckSessionExpiration.php` guardaba `now()` en sesión (`sistema_last_activity`) y luego llamaba `$ultimaActividad->lt(...)`. PHP serializa objetos al almacenarlos en sesión — al recuperarlo era un **string**, no un objeto Carbon, causando `Call to a member function lt() on string` al cargar cualquier página del panel.

**Solución:** Envolver `$ultimaActividad` en `Carbon::parse()` antes de llamar a `->lt()`.

## Qué se hizo el 2026-07-26 (décima sesión): 008-empleados-usuarios-erp completo (268/268 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, sigue a `007-clientes-vehiculos`). **Código commiteado y pusheado en `a604bd3`.**

**Backend:**
- **Migración `empleados`**: `taller_id` FK NOT NULL, `usuario_sistema_id` FK NULL a `usuarios_sistema`, `codigo`/`nombre`/`apellido`/`cargo`/`telefono`/`email`/`fecha_ingreso` (DATE NULL), `activo` BOOLEAN DEFAULT TRUE, soft delete, `UNIQUE(taller_id, codigo)` y `UNIQUE(taller_id, usuario_sistema_id)` (constraint compuesto simple, sin índice parcial: Postgres ya trata cada NULL como distinto en un unique compuesto, así que múltiples empleados sin usuario vinculado en el mismo taller no colisionan — a diferencia de `nit_ci` en `007`, que sí necesitó índice parcial por ser un unique de una sola columna).
- **Modelo `Empleado`** (`BelongsToTaller`, `SoftDeletes`, `scopeActivos()` mismo patrón que `Cliente`/`Vehiculo` para consumo futuro de `011`), `belongsTo(UsuarioSistema::class)` opcional, helper `tieneAcceso()`.
- **Decisión de diseño (no estaba en `plan.md` tal cual, pero se dedujo de él)**: se extrajo la lógica compartida de "crear identidad+usuario+credencial+rol+vincular+auditar" a una Action intermedia **`VincularAccesoEmpleadoAction`**, en vez de duplicarla entre "crear empleado con acceso desde cero" y "otorgar acceso después a un empleado que ya existía sin acceso" (este segundo caso no está descrito explícitamente como criterio de aceptación, pero se sigue directamente de que un empleado puede pasar de "sin acceso" a "con acceso" y el toggle de Filament vive en el mismo formulario tanto en crear como en editar). `CrearEmpleadoConAccesoAction` = crea el `Empleado` + llama a `VincularAccesoEmpleadoAction` en la misma transacción (rollback total si el rol no corresponde al taller o cualquier paso falla — verificado con test: ni el `Empleado` ni un `UsuarioSistema` huérfano quedan persistidos). `CrearEmpleadoSinAccesoAction`: un único INSERT, sin transacción propia (no hay nada que revertir).
- **`RestablecerPasswordUsuarioAction`/`ActivarDesactivarUsuarioSistemaAction`** en `app/Actions/Identidad/` (no en `Empleados/`): operan sobre `UsuarioSistema`, no sobre `Empleado` — un usuario del taller (p. ej. el owner) puede no tener nunca un registro `Empleado` asociado, así que viven junto a `CambiarPasswordAction`/`CrearUsuarioSistemaAction`, mismo criterio de ubicación por dominio del modelo que ya usaba el resto del proyecto. Ambas validan "pertenece al taller" vía `asignacionesVigentes()->contains(taller_id === $tallerId)`, no vía `Empleado` (para cubrir también usuarios sin registro de empleado). **Brecha documentada en el propio código** (docblock de `ActivarDesactivarUsuarioSistemaAction`): `activo` es una columna global de `UsuarioSistema`, no por taller — desactivar a un usuario que trabaja en dos talleres lo desactiva en ambos. El esquema de `empleados`/`asignaciones_rol` permite en teoría que un mismo `usuario_sistema_id` sea empleado de más de un taller (el UNIQUE solo evita duplicar el mismo par), pero no hay un "activo por taller" separado. Aceptado para el MVP, no se resuelve aquí.

**Filament (panel `/erp`):**
- **`EmpleadoResource`**: formulario con toggle "Tiene acceso al sistema" (no es una columna de `Empleado`, se intercepta en `Pages\CreateEmpleado`/`Pages\EditEmpleado` antes de tocar el modelo). En creación, dispara `CrearEmpleadoConAccesoAction`/`CrearEmpleadoSinAccesoAction`; en edición, si el empleado **todavía no tiene acceso** y el toggle se activa, dispara `VincularAccesoEmpleadoAction` (otorgar acceso después); si ya tiene acceso, el toggle queda deshabilitado (no se puede revocar ni re-vincular desde este formulario). La contraseña temporal generada se muestra una sola vez vía `Notification::make()->persistent()->body(...)` (verificado contra la documentación real de Filament v5 con Context7 antes de usar `persistent()`/`body()` — no había precedente de este patrón en el proyecto).
- **`UsuarioResource`**: sin create/edit/delete (la creación de cuenta siempre viene de `EmpleadoResource` o del flujo de aprobación de `004` para el owner). Lista usuarios con al menos una asignación de rol vigente en el taller activo (`whereHas('asignacionesRol', ...)` replicando exactamente el criterio de `AsignacionRol::estaVigente()` a nivel de query). Record actions "Restablecer contraseña" (muestra la nueva password temporal, persistente) y "Activar"/"Desactivar" (visibles mutuamente excluyentes según `activo`), ambas gateadas por `usuarios.gestionar` y delegando la regla de negocio a las Actions de `Identidad`.
- Permisos usados (`empleados.ver/crear/editar/eliminar`, `usuarios.ver/gestionar`) ya estaban en el catálogo de `002` desde el principio — no hizo falta agregar ningún permiso nuevo al seeder.

**29 tests Pest nuevos** en `tests/Feature/Empleados/`: `EmpleadoTest.php` (aislamiento por taller, auto-fill de `taller_id`, `UNIQUE(taller_id, codigo)`, `UNIQUE(taller_id, usuario_sistema_id)` — el criterio "un usuario sistema no puede ser empleado dos veces en el mismo taller" —, soft delete, `scopeActivos`, `tieneAcceso()`), `CrearEmpleadoActionsTest.php` (transacción completa con verificación de auditoría vía `Activity::where('description', ...)`, rol de otro taller rechazado con rollback total, username duplicado a nivel de BD fuerza rollback sin dejar usuario huérfano, `VincularAccesoEmpleadoAction` rechaza re-vincular un empleado que ya tiene acceso), `GestionCuentaActionsTest.php` (restablecer password exige cambio en el próximo login, rechaza si el usuario no pertenece al taller, **usuario desactivado no puede iniciar sesión** vía `auth('sistema')->attempt()` real — mismo patrón que `tests/Feature/Identidad/BloqueoIntentosFallidosTest.php` —, reactivar permite login de nuevo), `ResourcesAutorizacionTest.php` (permisos independientes + aislamiento por taller + páginas reales para ambos Resources), `CreacionFormularioTest.php` (3 tests Livewire: crear sin acceso, crear con acceso, otorgar acceso después desde `EditEmpleado`). **268/268 tests verdes en total** (239 previos + 29 nuevos), `laravel/pint` sin cambios pendientes.
- Verificado con `php artisan serve` + `curl` contra la BD de desarrollo real: `/erp/empleados` y `/erp/usuarios` sin sesión → 302 (no 500). **No hubo click-through en navegador real** (`claude-in-chrome` no disponible en esta sesión, mismo motivo que sesiones previas) — el toggle dinámico del formulario (mostrar/ocultar username y rol según "tiene acceso") no se verificó visualmente, solo por lectura de código + los 3 tests Livewire que sí ejercitan el guardado real del formulario en sus dos ramas (con/sin acceso) más el caso de edición.

### Qué falta de `008` (fuera de alcance a propósito)

- **Auditoría real (`015-auditoria`)**: igual que `003`/`004`, se usa `activity()` de `spatie/laravel-activitylog` como stand-in; la tabla `auditoria_eventos` no existe todavía.
- **"Activo por taller" para usuarios con más de un taller**: brecha documentada arriba — `UsuarioSistema.activo` es global, no hay forma de desactivar un usuario solo en un taller sin afectar los demás. Caso raro, no bloqueante para el MVP.
- Verificación visual en navegador real (ver arriba).
- **Código de `008` commiteado y pusheado en `a604bd3` en `origin/specs/planificacion`.**

## Qué se hizo el 2026-07-26 (novena sesión): 007-clientes-vehiculos completo (239/239 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 7, primero de las features `007`-`015` que reutilizan Filament + `BelongsToTaller` ya configurados desde el paso 0). Antes de empezar se confirmó que `006-resenas-favoritos` (sesión anterior) ya estaba commiteado y pusheado (`e8aed6c` en `origin/specs/planificacion`) — `resume.md` había quedado desactualizado en ese único punto, no hacía falta commitear nada.

**Backend:**
- **Migraciones** `clientes` (`UNIQUE(taller_id, codigo)`, índice único parcial `UNIQUE(taller_id, nit_ci) WHERE nit_ci IS NOT NULL`, CHECK `tipo_persona IN ('NATURAL','JURIDICA')`, soft delete) y `vehiculos` (`UNIQUE(taller_id, placa)`, CHECK de regex de placa `^[A-Z]{3}[0-9]{3}$`, CHECK de año, CHECK de `tipo_vehiculo`, CHECK `kilometraje >= 0`, soft delete). Sin partial-index sobre `deleted_at` para `codigo`/`placa` a propósito: `constitution.md` §3.8 exige que los códigos operativos (incluye código de cliente y placa) **nunca sean reutilizables**, ni siquiera tras soft delete.
- **Modelos** `Cliente`/`Vehiculo`, ambos con `BelongsToTaller` + `SoftDeletes`. **Mutators con `Attribute::make()`** (primer uso de esa sintaxis en el proyecto, los modelos previos no tenían mutators): `Cliente::nitCi()` normaliza `''` a `null` al guardar (protege el índice único parcial sin importar la vía de escritura — Filament, tinker, seeders), `Vehiculo::placa()` normaliza a mayúsculas sin guion (`ABC123`) sin importar cómo se haya tipeado, `Vehiculo::placaFormateada()` accessor de solo lectura para mostrar `ABC-123`. `scopeActivos()` en ambos modelos (consumidor real recién en `011`/`012`, mismo patrón que `Taller::scopeVisibleEnMarketplace` de `003` se escribió antes de que `005` lo consumiera).
- **Form Requests** `ClienteRequest`/`VehiculoRequest` (sin controlador/ruta propia todavía, mismo patrón que `GuardarHorarioTallerRequest` de `003` — Filament no las instancia directamente, tiene su propio Schema con reglas equivalentes): unicidad de `codigo`/`nit_ci`/`placa` acotada a `session('taller_activo_id')` (no a un parámetro del request), `VehiculoRequest::prepareForValidation()` normaliza la placa antes de validar el regex, `cliente_id` valida pertenencia al taller activo vía `Rule::exists()->where(...)`.
- **Filament** `ClienteResource` (formulario dinámico natural/jurídica con `->live()`, tabla con nombre/razón social combinado, columna de conteo de vehículos) y `VehiculoResource` (placa con `->live(onBlur: true)` + `afterStateUpdated()` que normaliza el valor en el propio estado de Livewire antes de que corra la validación — evita depender de la incertidumbre de si `dehydrateStateUsing` corre antes o después de la validación en Filament v5). Ambos sin `getEloquentQuery()` manual: a diferencia de `TallerResource`/`RolResource` (que filtran taller a mano porque sus modelos no usan el trait), `Cliente`/`Vehiculo` sí usan `BelongsToTaller`, así que el global scope ya resuelve el filtrado.
- **Bug real evitado (detectado leyendo el código de Filament antes de confiar en `scopedUnique()`, no después)**: `TextInput::scopedUnique()` (`vendor/filament/forms/.../CanBeValidated.php`) arma la query como `$model::query()->where($column, $value)`. Si el valor de un campo opcional (`nit_ci`) llegara como `null` real al validador y el campo tuviera la regla `nullable`, Laravel **saltea por completo** la regla `unique` (confirmado leyendo `Validator::isNotNullIfMarkedAsNullable()`) — pero si llegara como `''` (string vacío, lo típico de un input HTML dejado en blanco), Laravel **no** lo saltea, y la query sería `WHERE nit_ci = ''`. Como el mutator de `Cliente::nitCi()` garantiza que ningún registro llega a tener `''` real en BD (siempre `null`), esa comparación nunca hace match — sin importar cuántos clientes con NIT/CI en blanco existan ya, un valor `''` entrante jamás colisiona con un valor `NULL` almacenado. **Verificado con 3 tests Livewire reales** (`CreacionFormularioTest.php`): crear un cliente sin NIT/CI, crear un segundo cliente sin NIT/CI en el mismo taller (el caso que habría fallado sin el mutator) y rechazar un NIT/CI real duplicado — los tres pasan.
- **27 tests Pest nuevos**: `tests/Feature/Clientes/` (`ClienteTest.php`: aislamiento por taller, CHECKs de BD, soft delete, normalización de `nit_ci`, `scopeActivos`; `ClienteRequestTest.php`: unicidad de `codigo`/`nit_ci` por taller vía Form Request, email inválido; `ResourcesAutorizacionTest.php`: permisos independientes ver/crear/editar/eliminar + páginas reales index/create/edit vía HTTP; `CreacionFormularioTest.php`: los 3 tests Livewire del bug evitado arriba, más creación de vehículo con normalización de placa) y `tests/Feature/Vehiculos/` (`VehiculoTest.php`, `VehiculoRequestTest.php`: mismos criterios para placa/año/kilometraje/tipo_vehiculo, más "cliente pertenece a otro taller" rechazado). **239/239 tests verdes en total** (197 previos + 42 nuevos — incluye 4 tests de Livewire nuevos que no existían como patrón en el proyecto hasta ahora), `laravel/pint` sin pendientes (con auto-fix de imports en un archivo, verificado que la suite siguió verde después).
- Migraciones corridas contra la BD de desarrollo real (no solo `taller_test`), verificado con tinker: normalización de placa, dos clientes con `nit_ci` nulo conviven en el mismo taller.

### Qué falta de `007` (fuera de alcance a propósito)

- **Bloqueo de soft delete por hijos activos** (`constitution.md` §3.13: "no se borra si hay órdenes en curso") — no se puede implementar todavía porque `ordenes_trabajo`/`notas_venta` no existen (`011`/`012`, specs posteriores en el orden del proyecto). El soft delete en sí (no bloqueante) ya funciona. Mismo patrón de brecha diferida que `003-gestion-talleres` dejó documentado para el conteo de hijas activas de un taller.
- **Filtrado real de "inactivo no seleccionable en orden nueva"**: se agregó `scopeActivos()` en ambos modelos como base reutilizable, pero el consumidor real (el selector de cliente/vehículo al crear una orden) es de `011-ordenes-trabajo`.
- Verificación visual en navegador real: no se hizo (mismo motivo que sesiones previas, `claude-in-chrome` no disponible) — esta vez la cobertura HTTP es más fuerte que en sesiones anteriores porque se agregaron tests Livewire reales que ejercitan el guardado del formulario (`fillForm()->call('create')`), no solo la carga de la página.
- **Código de `007` commiteado y pusheado en `b8802c3` en `origin/specs/planificacion`.**

## Qué se hizo el 2026-07-26 (octava sesión): 006-resenas-favoritos completo (197/197 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 6, depende de 001+003, ambos completos). Primera sesión que agrega escritura real al marketplace (hasta ahora todo era lectura pública) y el primer uso real de `Alpine.store('ui').addToast()` (existía desde 016 sin consumidor).

**Backend:**
- **Migraciones** `resenas` (CHECK `calificacion BETWEEN 1 AND 5`, CHECK estado `PUBLICADA|OCULTA|REPORTADA`, `UNIQUE(usuario_marketplace_id, taller_id)`, soft delete) y `favoritos` (mismo UNIQUE, sin `updated_at` — solo alta/baja, hard delete). `orden_trabajo_id` en `resenas` es `unsignedBigInteger` nullable **sin FK todavía** (la tabla `ordenes_trabajo` no existe hasta `011`, mucho después en el orden del proyecto) — documentado en la migración para que `011` la agregue cuando exista.
- **Modelos** `Resena` (`SoftDeletes`) y `Favorito` (`const UPDATED_AT = null`), con sus factories. Relaciones `resenas()`/`favoritos()` agregadas a `UsuarioMarketplace`.
- **Eventos `ResenaGuardada`/`ResenaEliminada`** + **`RecalcularCalificacionTallerListener`** (auto-descubierto vía `handle*`, dos métodos `handleResenaGuardada`/`handleResenaEliminada` — mismo patrón multi-`handle` que ya usaba `app/Listeners/Identidad/`). Síncrono (constitution.md §3.12): se dispara dentro del mismo `DB::transaction()` que la Action, sin `ShouldQueue`.
- **3 Actions en `app/Actions/Resenas/`**: `GuardarResenaAction` (upsert real por `UNIQUE(usuario,taller)`; **al editar no toca `estado`** — evita que el autor revierta silenciosamente una moderación previa OCULTA/REPORTADA), `EliminarResenaAction` (soft delete + verifica pertenencia), `ModerarResenaAction` (super admin, gate `moderacion.resenas`, reutiliza `ResenaGuardada` para disparar el mismo recálculo que el flujo público).
- **Bug real encontrado con tinker antes de confiar en los tests**: el listener llamaba a `$taller->update([...])`, pero `calificacion_promedio`/`cantidad_resenas` **no están en `Taller::$fillable`** (a propósito — son columnas derivadas, nunca deben aceptarse desde un formulario) — Eloquent ignoraba esas claves en silencio, sin error, y el taller se quedaba siempre en 0/0.00 pese a que la reseña sí se creaba. Corregido con `forceFill([...])->save()` en el listener. Encontrado reproduciendo el flujo completo por tinker contra la BD real antes de escribir los tests — si el primer indicio hubiera sido un test rojo habría sido igual de válido, pero tinker fue más rápido para aislarlo.
- **Segundo bug real, encontrado por un test**: no existía ninguna ruta protegida por `auth:web` con método `GET` antes de esta sesión (solo `POST /auth/logout`) — un visitante sin sesión pidiendo `GET /dashboard` producía **500** (`RouteNotFoundException: Route [login] not defined`), no un 302 limpio, porque `Illuminate\Auth\Middleware\Authenticate` intenta redirigir a `route('login')` por defecto y el marketplace no tiene formulario de login propio (solo Google OAuth). Corregido con `Authenticate::redirectUsing(fn () => route('home'))` en `AppServiceProvider::boot()`.
- **`ResenaApiController@storeOrUpdate/destroy`**, **`FavoritoApiController@store/destroy`** (sin Action propia — alta/baja de favorito es un `firstOrCreate`/`delete` idempotente sin más regla de negocio, a diferencia de reseñas), **`DashboardController@index`** (`/dashboard`, "Mis Favoritos" + "Mis Reseñas").
- **Rutas `POST /api/resenas`, `DELETE /api/resenas/{resena}`, `POST /api/favoritos`, `POST /api/favoritos/delete`** con `['web', 'auth:web', 'throttle:10,1']` — **el grupo `web` es necesario a propósito**: el grupo `api` (`bootstrap/app.php`) no arranca sesión ni CSRF por defecto (a diferencia de `/api/talleres/search` de `005`, que es público y stateless), así que sin el middleware `web` explícito el guard `auth:web` no podía leer la sesión del usuario marketplace autenticado por OAuth. Como consecuencia, el fetch de Alpine debe enviar `X-CSRF-TOKEN` — se agregó `<meta name="csrf-token">` a `marketplace/layouts/{app,auth}.blade.php`, leído por los componentes Alpine nuevos.
- **`App\Filament\Admin\Resources\ModeracionResenasResource`**: sin create/edit/delete (solo cambia `estado` vía `ModerarResenaAction`), gate `moderacion.resenas`, filtro por estado, 3 acciones (Ocultar/Marcar como reportada/Publicar) — el criterio de aceptación permite las tres transiciones, aunque `tasks.md` solo mencionaba "ocultar/publicar" explícitamente.

**Frontend:**
- **Alpine `resenaForm()`/`favoritoToggle()`** (`resources/js/alpine/components/`) + registrados en `app.js`. `resenaForm` reutiliza `<x-marketplace.star-rating readonly="false">` de `016` sin reconstruirlo — se le agregó una única línea (`$dispatch('star-rating-changed', {{ $i }})` en el click de cada estrella) para que el componente padre pueda escuchar el cambio, ya que el picker interactivo vive en un scope Alpine anidado (`x-data` propio) que no comparte estado con el formulario que lo envuelve.
- **`<x-marketplace.resena-form>`** ("Escribir reseña"/"Mi reseña", con resumen de estrellas reactivo dibujado a mano con `x-for` — no reutiliza el modo `readonly` de `<x-marketplace.star-rating>` porque ese renderiza a partir de un valor fijo del servidor, no reactivo al estado de Alpine tras guardar sin recargar), **`<x-marketplace.favorito-button>`** (corazón lleno/vacío), **`<x-marketplace.toast-container>`** (primer consumidor real de `Alpine.store('ui').addToast()`, que existía desde `005`/`016` sin nada que lo renderizara).
- **`TallerPerfilController@show`** ahora pasa `resenas` (públicas, `PUBLICADA`, excluye la propia para no duplicarla con "Mi reseña"), `miResena`, `esFavorito`. **`workshops/show.blade.php`** actualizado: reseñas reales vía `<x-marketplace.review-card>` (ya existía desde `016`, sin cambios), formulario condicionado a `@auth('web')`, botón de favorito junto al header.
- **`marketplace/dashboard/index.blade.php`** (nuevo, layout `auth.blade.php` — primer consumidor real de ese layout, existía desde `005`/`016` sin ninguna ruta): grid "Mis Favoritos" (`<x-marketplace.workshop-card>` + botón quitar) y lista "Mis Reseñas" (reutiliza `<x-marketplace.resena-form>` por cada una, con editar/eliminar inline).
- `nav.blade.php`/`auth.blade.php`: el nombre del usuario y el texto "próximamente" ahora enlazan a `/dashboard`.
- **27 tests Pest nuevos** en `tests/Feature/Resenas/`: unicidad (upsert no duplica), solo `PUBLICADA` cuenta en la calificación (BD real, no solo el listener aislado), recálculo síncrono en creación/eliminación, 401 sin sesión, guard `sistema` no puede usar rutas de guard `web`, DELETE de reseña/favorito ajenos rechazado, DELETE de favorito inexistente idempotente (200), throttle 10/min, moderación (gate, transición de estado, recálculo tras moderar), render real del perfil público (con/sin reseña propia, oculta no aparece, botón de favorito refleja estado), dashboard (vacío y con datos, requiere sesión). **197/197 tests verdes en total**, `laravel/pint` sin pendientes.
- Verificado con `php artisan serve` + `curl` contra la BD de desarrollo real: `/dashboard` sin sesión → 302 a `/` (ya no 500), `/admin/moderacion-resenas` sin sesión → 302, `/talleres/{slug-real-creado-y-luego-borrado}` con reseñas vacías → 200 con "Aún no hay reseñas". **No hubo click-through en navegador real** (`claude-in-chrome` no disponible en esta sesión, mismo motivo que sesiones previas) — la interactividad de Alpine (guardar/editar/eliminar reseña, toggle de favorito, toasts) no se verificó visualmente, solo por lectura de código + build de Vite sin errores + los 197 tests ejercitando el HTTP round-trip completo.

### Qué falta de `006` (fuera de alcance a propósito)

- `orden_trabajo_id` en `resenas` sin FK real todavía — se agrega cuando `011-ordenes-trabajo` cree esa tabla.
- Verificación visual en navegador real (ver arriba).
- Los permisos `marketplace.resenar`/`marketplace.favoritos` del catálogo de `002` siguen sin usarse (brecha estructural ya documentada en la tercera sesión: `MARKETPLACE_USER` no se puede asignar vía `asignaciones_rol` porque esa tabla es FK a `usuarios_sistema`, no a `usuarios_marketplace`) — esta sesión confirma la recomendación ya tomada: la autorización de reseñas/favoritos se resuelve solo con `auth:web` (autenticación), sin capa de permisos adicional.

## Qué se hizo el 2026-07-26 (séptima sesión): 005-marketplace-busqueda-perfil + 016-ui-design-system completos (172/172 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 5, implementado en paralelo como indica la tabla). Primera sesión que toca el marketplace público real (home, búsqueda, perfil) — hasta ahora solo existía el prototipo `welcome.blade.php` y el formulario de `004`.

**016 primero (infraestructura visual que 005 consume), luego 005 sobre esa base:**
- **Tokens Awesomic** en `resources/css/app.css`: los 13 colores, tipografía (`--text-*`/`--text-*--line-height`, sintaxis real de Tailwind v4 verificada contra Context7), spacing, radii, sombras, clases `.badge-*`. Fuente **DM Sans** vía `@fontsource/dm-sans` (ya estaba en `package.json`, no se usaba) importada directo en CSS — se quitó `bunny('Instrument Sans', ...)` de `vite.config.js` (fuente default del skeleton de Laravel, ya no aplica).
- **Corrección de spec detectada**: `<x-badge type="danger">` no debía ser filled-ember (así estaba en mi primer borrador) — el criterio real de `016-spec.md` separa "badge de estado" (outlined, transparente) de "badge de acento" (`type="accent"`, único filled ember). Corregido antes de construir el componente.
- **Filament v5 real (no la sintaxis de plan.md sin verificar)**: `php artisan make:filament-theme {admin,erp}` generó `resources/css/filament/{admin,erp}/theme.css` + los registró en `vite.config.js`. `->viteTheme(...)` y `->font('DM Sans')` agregados a mano en ambos `PanelProvider` (el comando detectó por error un comentario viejo que mencionaba `viteTheme()` y no lo agregó solo en `AdminPanelProvider`; sí lo agregó — duplicado — en `ErpPanelProvider`, se limpió el duplicado). `->colors()` de la quinta sesión se mantiene intacto como fuente de verdad de la paleta; el theme.css no repite colores, solo trae la tipografía/estructura base de Filament + `@source` para que Tailwind escanee las vistas de cada panel.
- **Alpine.js no estaba instalado** (`constitution.md` lo da por hecho en el stack pero nunca se había instalado) — `npm install alpinejs`. `resources/js/app.js` ahora importa Alpine + Leaflet (npm, no CDN — reemplaza el patrón CDN de `welcome.blade.php`/`solicitudes/crear.blade.php`) + los stores/componentes.
- **Alpine stores/componentes** (`resources/js/alpine/`): `store.js` (`Alpine.store('search')` con fetch a la API + `Alpine.store('ui')`), `components/map.js` (`map()` sincronizado con `$store.search.results` vía `$watch`, más `singleMap()` para el perfil de un solo taller — no dependen del store de búsqueda), `components/search.js` (`searchForm()`, dispara la primera búsqueda al montar), `components/rating.js` (`starRating()`, modo interactivo sin uso real todavía — es para `006`).
- **Layouts marketplace** (`resources/views/marketplace/layouts/`): `app.blade.php` (nav+footer, usado por home/búsqueda/perfil), `guest.blade.php` (centrado, sin nav — reservado para login/404, nada lo usa todavía), `auth.blade.php` (variante con datos del usuario, tampoco tiene página real todavía — el dashboard de favoritos/reseñas es de `006`).
- **9 componentes Blade globales** (`resources/views/components/`): badge, button, card, input, select, empty-state, alert, modal, pagination. Más `skeleton.blade.php` (no está en el catálogo de `016-plan.md` pero lo exige `constitution.md` §5 para loading states).
- **8 componentes marketplace** (`resources/views/components/marketplace/`): nav, footer, workshop-card, search-filters, map, star-rating, review-card, stats-block. `review-card` acepta cualquier shape (`data_get`) porque el modelo `Resena` de `006` no existe todavía — documentado en el propio componente para que `006` lo reutilice sin tocarlo.
- **Decisión**: las tarjetas de resultados de la búsqueda EN VIVO (`/talleres/buscar`, sección del home) no usan `<x-workshop-card>` — no se puede invocar un componente Blade desde JS/Alpine una vez que la página ya se renderizó. Se escribió el markup equivalente inline en un `<template x-for>` dentro de `resources/views/marketplace/partials/search-experience.blade.php` (parcial compartido entre home y `/talleres/buscar` vía `@include`, no `<x-...>` porque no vive en `components/`). `<x-workshop-card>` sí se usa de verdad, server-rendered, en la sección "Talleres similares" del perfil público.

**005 sobre esa base:**
- **`Taller::scopeActivosVisibles()`**: alias de `scopeVisibleEnMarketplace()` (ya existía desde `003`) — mismo predicado, una sola fuente de verdad, para no duplicar la condición en dos sitios con nombres distintos.
- **`Taller::estaAbiertoAhora()`**: usa `America/La_Paz`, lee `$this->horarios` (eager-loadable, sin N+1 en listados). **`Taller::toSearchJsonResponse()`**: arma el JSON exacto del spec (incluye `descripcion_corta` vía `Str::limit`, ausente como columna propia — se deriva de `descripcion`).
- **Dos bugs reales encontrados en infraestructura de `017` que `005` fue la primera feature en ejercitar de verdad** (los scopes `cercanoA`/`conDistanciaA` de `HasGeolocation` existían desde la primera sesión pero nunca se habían usado ni testeado):
  1. **`scopeConDistanciaA()` perdía todas las columnas del modelo**: `selectRaw('... as distancia')` sin un `select`/`addSelect` previo reemplaza el `SELECT *` implícito por la sola columna calculada (gotcha conocido de Eloquent) — el modelo se hidrataba solo con `distancia`, todo lo demás en `null`. Corregido con `addSelect("{$table}.*")` antes del `selectRaw`.
  2. **`scopeCercanoA()` no filtraba nada en la práctica**: `ST_DWithin` sobre columnas `geometry` (no `geography`) mide en las unidades del SRID — en 4326 eso es **grados**, no metros. Un radio de "5000" se interpretaba como 5000 grados (cubre el planeta entero). Verificado con dos puntos a 546km de distancia que pasaban un filtro de 5km sin el fix. Corregido envolviendo ambos lados en `geography(...)`, que fuerza el cálculo esférico en metros. Sin este fix, el filtro de radio de la búsqueda geográfica —la razón de ser de la feature— no habría funcionado nunca en producción pese a no dar ningún error visible.
- **`TallerBusquedaApiController@search`**: filtros lat/lon/radio (reutiliza `cercanoA`/`conDistanciaA`, ya corregidos), categoria (slug), `q` (ILIKE), `min_calificacion`, `open_now` (JOIN a `talleres_horarios` del día de hoy, igual que documentaba `005-plan.md`), `sort` (cercania/calificacion/resenas). Cuidado con el `JOIN` de `open_now` + `conDistanciaA` a la vez: ambos intentan agregar `talleres.*` al select — se evita el duplicado condicionando el segundo `addSelect` a que el primero no haya corrido.
- **Ruta `GET /api/talleres/search`** con `throttle:30,1` en `routes/api.php` — **se eliminó la ruta prototipo `GET /talleres` y su controlador `TallerController`** (pre-specs, devolvía columnas que ya no existen). También se eliminó `resources/views/welcome.blade.php` (dependía de esa ruta muerta).
- **`HomeController@index`** (nuevo — antes `/` era una closure devolviendo `welcome`), **`TallerBusquedaController@index`** (`/talleres/buscar`, no ejecuta query — Alpine la dispara desde el cliente, tal como pide `005-plan.md`), **`TallerPerfilController@show`** (`/talleres/{slug}`, `activosVisibles()->firstOrFail()` da 404 automático para inactivo/suspendido/no-visible/soft-deleted/inexistente sin distinguir el caso — evita filtrar esa información).
- **Vistas**: `marketplace/home.blade.php` (hero con input de búsqueda + CTA, banda de categorías, stats row con COUNT/AVG reales, sección "breakthrough" full-bleed decorativa, y la experiencia de búsqueda embebida), `marketplace/search/index.blade.php` (misma experiencia, página dedicada), `marketplace/workshops/show.blade.php` (datos completos + mapa Leaflet de un solo punto + horarios + placeholder de reseñas vacío con `<x-empty-state>`, ya que `Resena` es de `006` + talleres similares).
- **21 tests Pest nuevos** en `tests/Feature/Marketplace/`: búsqueda nunca devuelve inactivo/no-visible/suspendido/borrado, todos los campos del JSON presentes, filtro de radio (el que detectó el bug de `ST_DWithin`), filtros combinados (categoría/texto/calificación), `open_now`, rate limiting 30/min, perfil 404 en los 5 casos (inactivo/suspendido/no-visible/borrado/inexistente), `estaAbiertoAhora()` en los 4 bordes (uso de `Carbon::setTestNow()` a mediodía fijo para no depender de la hora real de ejecución — evita un test frágil cerca de medianoche), home/búsqueda cargan con las vistas correctas, todos los componentes Blade nuevos renderizan sin errores. **172/172 tests verdes en total** (151 previos + 21 nuevos), `laravel/pint` sin pendientes.
- Verificado con `php artisan serve` + `curl` contra la BD de desarrollo real (no solo contra `taller_test`): `/`, `/talleres/buscar`, `/talleres/{slug-real-creado-y-luego-borrado}`, `/api/talleres/search`, `/admin/login`, `/erp/login`, `/solicitudes-taller/nueva` — todos 200 (o 404 donde corresponde), sin excepciones en el HTML. **No hubo click-through en navegador real** (`claude-in-chrome` no disponible en esta sesión, mismo motivo que sesiones previas) — la interactividad de Alpine (mapa, filtros, fetch) no se verificó visualmente, solo por lectura de código + que los assets compilan y las páginas cargan sin error de servidor.

### Qué falta de `005`/`016` (fuera de alcance a propósito)

- **Dashboard del usuario marketplace** (`/dashboard`, favoritos + reseñas): depende de `006-resenas-favoritos`, que no existe todavía. El layout `auth.blade.php` que lo usaría ya está construido, pero sin ruta/controlador/vista real.
- **Reseñas reales en el perfil público**: la sección existe pero muestra `<x-empty-state>` estático — `<x-marketplace.review-card>` está listo para que `006` lo use tal cual.
- **`Alpine.data('starRating')` interactivo**: construido pero sin ningún formulario real que lo use (escribir reseñas es de `006`).
- Tema visual no verificado en navegador real (ver arriba) — el CSS compila y las clases Tailwind existen, pero no hubo inspección visual de que el layout/spacing final coincida pixel a pixel con el spec Awesomic.

## Qué se hizo el 2026-07-26 (sexta sesión): 004-solicitud-alta-taller completo (backend + UI pública + Filament, 151/151 tests verdes)

Siguiendo el orden estricto de `AGENTS.md` (paso 4, después de 002/003). Backend, UI pública y UI Filament implementados en la misma sesión (no se dividió en "backend primero, UI después" como en 001-003).

**Código nuevo:**
- **Migraciones** `2026_07_26_070001_create_solicitudes_taller_table` (con los 2 CHECK de coherencia `COMPLETADA`→`taller_id NOT NULL` / `RECHAZADA`→`taller_id NULL`, CHECK de lat/lon nullable, índice GIST) y `..._070002_create_solicitudes_taller_historial_table` (append-only, sin `updated_at`).
- **Modelos** `SolicitudTaller` (usa `HasGeolocation`, igual patrón que `Taller`) y `SolicitudTallerHistorial`.
- **Form Request** `App\Http\Requests\Solicitudes\CrearSolicitudTallerRequest` (público, `authorize()` siempre `true`): solo exige los mínimos del spec (datos del solicitante + nombre del taller); ubicación/categoría opcionales.
- **6 Actions en `app/Actions/Solicitudes/`**: `CrearSolicitudTallerAction` (genera `token_publico` con `Str::uuid()`, no lo genera la BD — así se puede devolver de inmediato sin un segundo round-trip), `IniciarRevisionSolicitudAction`, `AprobarSinCompletarSolicitudAction`, `AprobarYCompletarSolicitudAction`, `RechazarSolicitudAction`, `CancelarSolicitudAction`. Cada Action de transición declara su propio `ORIGENES_VALIDOS` (const) y usa `lockForUpdate()` releyendo la fila dentro de la transacción — el mismo patrón protege contra doble aprobación aunque se llame dos veces con la misma referencia de objeto en memoria (verificado con test).
- **`AprobarYCompletarSolicitudAction`** es la más compleja: crea el `Taller` dentro de la misma transacción, acepta `$tallerData` (override parcial: nombre/dirección/teléfono/email/lat/lon) para que el super admin pueda editar antes de crear — cualquier campo ausente cae al valor de la solicitud original. Exige **ambos** permisos `solicitudes.aprobar` y `solicitudes.crear_taller` (separados a propósito en el catálogo de `002`, para poder delegar revisión sin delegar creación del tenant). Si falla la creación del taller (ej. CHECK de lat/lon violado), rollback total verificado con test.
- **Auditoría**: vía `activity()` (spatie/laravel-activitylog), mismo patrón que las Actions de `003-gestion-talleres` — no la tabla `auditoria_eventos` de `015` (no existe todavía).
- **Rutas públicas** (`routes/web.php`, sin autenticación): `GET /solicitudes-taller/nueva`, `POST /solicitudes-taller`, `GET /solicitudes-taller/{token}`, `POST /solicitudes-taller/{token}/cancelar`. Todas resuelven por `token_publico`, nunca por `id` (sin route-model-binding implícito, que expondría el id interno).
- **Vistas Blade públicas**: `resources/views/layouts/marketplace.blade.php` (layout mínimo nuevo, primer uso de `@vite` fuera de `welcome.blade.php`), `resources/views/solicitudes/crear.blade.php` (formulario multi-paso: solicitante → taller → mapa opcional → confirmación, navegación con JS vanilla — **no Alpine.js**: el paquete todavía no está instalado, se instala junto con `016-ui-design-system`/`005`, y forzar Alpine ahora habría anticipado esa decisión) y `resources/views/solicitudes/seguimiento.blade.php` (estado con badge de color, historial, botón cancelar si `PENDIENTE`/`EN_REVISION`). Mapa con Leaflet vía CDN (mismo patrón que el prototipo `welcome.blade.php`, no vía npm), clic para marcar ubicación o botón "usar mi ubicación actual" (`navigator.geolocation`); es opcional, el spec permite lat/lon nulos en la creación.
- **`App\Filament\Admin\Resources\SolicitudTallerResource`**: sin páginas create/edit (la solicitud solo se crea desde el formulario público y solo cambia de estado vía Actions, nunca editando columnas sueltas sin más — `canCreate()`/`canEdit()`/`canDelete()` = `false`). Tabla con filtro por estado, `ViewAction` con infolist (incluye `RepeatableEntry` del historial) y 4 record actions: "Iniciar revisión", "Aprobar (sin crear taller aún)", "Aprobar y crear taller" (modal con `fillForm()` pre-cargando nombre/dirección/teléfono/email/lat/lon editables, requiere lat/lon), "Rechazar" (modal con `Textarea` obligatorio). Gate `solicitudes.ver` para ver el listado.
- **44 tests Pest nuevos** en `tests/Feature/Solicitudes/`: creación (token único, no expone id, historial inicial), las 11 transiciones válidas de la tabla del spec (una por fila) + transiciones inválidas rechazadas, permisos por Action, `AprobarYCompletarSolicitudAction` a fondo (crea taller en la transacción, permite editar datos, exige lat/lon, rollback total si el `Taller` viola un CHECK, protección de doble aprobación), 8 tests de CHECKs de BD (completada-implica-taller, rechazada-implica-sin-taller, catálogo de estados, lat/lon nullable pero acotado, unicidad de `token_publico`/`taller_id`), autorización del Resource + carga real de la página `/admin`, y flujo público end-to-end (formulario→creación→redirect a seguimiento por token, validación de mínimos, cancelación, 404 en token inexistente). **151/151 tests Pest verdes en total** (107 previos + 44 nuevos), `laravel/pint` sin pendientes.
- Verificado con `php artisan serve` + `curl` contra la BD de desarrollo real: `GET /solicitudes-taller/nueva` → 200 con el CSS de Tailwind compilado enlazado (no solo el welcome.blade.php de siempre), `GET /admin/solicitudes-taller` sin sesión → 302 a login (no 500), `GET /solicitudes-taller/{token-inexistente}` → 404. No hubo click-through completo en navegador real (mismo motivo que en la quinta sesión: `claude-in-chrome` no disponible en esta sesión) — los 44 tests sí ejercitan un HTTP round-trip completo del flujo público (POST real con `Illuminate\Foundation\Testing\TestCase`, no solo llamadas directas a las Actions).

### Decisión tomada sin preguntar (documentada aquí para que quede explícita)

**"Aprobar sin completar" sí tiene su propio botón en el admin**, aunque `004-plan.md` en su sección de Filament solo menciona explícitamente "Iniciar Revisión" / "Aprobar y Crear Taller" / "Rechazar". La tabla de transiciones del `spec.md` sí define `PENDIENTE|EN_REVISION -> APROBADA` como transición válida y necesaria (paso intermedio antes de `APROBADA -> COMPLETADA`); omitir el botón habría dejado ese estado inalcanzable desde la UI. Se agregó como acción adicional "Aprobar (sin crear taller aún)".

### Qué falta de `004` (fuera de alcance a propósito)

- **Detección de duplicados** (mismo `osm_id`/nombre/teléfono cercano): el propio criterio de aceptación la marca como opcional e informativa, condicionada a "que el negocio lo decida explícitamente" — no se tomó esa decisión en esta sesión, así que no se implementó. Si se decide más adelante, es un scope pequeño y aislado (un query adicional en `CrearSolicitudTallerAction` o un job posterior).
- Notificación automática al solicitante por email — explícitamente fuera de alcance del MVP en el propio `spec.md` ("revisar si `014-notificaciones` lo cubre").
- CSS/tema Tailwind completo del design system (`016-ui-design-system`) — las vistas públicas de esta sesión usan Tailwind utility classes directas (zinc/red/green/blue/amber genéricos de Tailwind), no la paleta Awesomic de 13 colores todavía; eso se hace junto con `005`. El layout `resources/views/layouts/marketplace.blade.php` es intencionalmente mínimo y se espera que se reemplace o extienda cuando llegue `016`+`005`.

## Qué se hizo el 2026-07-26 (quinta sesión): UI real de 017/001/002/003 (107/107 tests verdes)

El usuario pidió cerrar todo lo pendiente de UI antes de arrancar `004-solicitud-alta-taller`. Alcance confirmado explícitamente: terminar `017-infraestructura-sistema` (middleware, paneles, paleta) + construir los Resources de Filament pendientes de `001`/`002`/`003`. Plan completo en el historial de la sesión (no quedó archivo de plan en el repo, era `~/.claude/plans/`).

**Housekeeping de documentación primero** (varios documentos asumían `spatie/laravel-permission`, removido desde la sesión de `002`, o dejaban decisiones sin resolver):
- `constitution.md` §1 y §5 actualizados: roles/permisos 100% custom (ya no menciona Spatie), paleta Awesomic real en vez de la línea azul-marino/naranja obsoleta.
- `017-infraestructura-sistema/plan.md`: corregido el bloque de `SetTallerActivo` (ya no llama a `Spatie\Permission\PermissionRegistrar`, que no existe), paths de `AdminPanelProvider` a `Filament/Admin/...`, y **un bug real en el propio plan** (`->colors([... 'gray' => fn () => [...]])` — envolver el valor de `gray` en una closure hace explotar `ColorManager::getColors()` con `array_map(): Argument #2 must be of type array, Closure given`; el array debe ir plano). `017-tasks.md`, `002-plan.md`/`tasks.md`, `003-tasks.md` actualizados para reflejar el estado real.

**Decisiones de arquitectura tomadas (documentadas en el código, no solo aquí):**
1. **No se usa `->tenant()` nativo de Filament** — ya existe `BelongsToTaller` + sesión funcionando; usar ambos duplicaría la fuente de verdad del tenant activo.
2. **`SetTallerActivo` solo en `/erp`, no en `/admin`** — el Super Admin no opera "dentro" de un taller.
3. **Autorización de Resources vía `canViewAny()`/`canCreate()`/`canEdit()`/`canDelete()` estáticos** que llaman a `tienePermiso()`/`esSuperAdmin()` directamente — no Policies de Laravel (no existían, y el catálogo de permisos ya resuelve la granularidad).
4. **`AdminPanelProvider` ahora descubre en `app/Filament/Admin/...`** (antes `app/Filament/Resources` a secas, inconsistente con `Erp/...`).
5. **Paleta Awesomic: solo el array `->colors([...])` de Filament, sin los CSS theme files (`viteTheme()`)** — eso se hace junto con `005`+`016` per `AGENTS.md`.
6. **Nueva Action `CambiarEstadoTallerAction`** (no existía): transiciona `estado` ACTIVO/INACTIVO/SUSPENDIDO, solo Super Admin (único permiso del catálogo para esto es `admin.talleres.suspender`).
7. **`TallerResource` de `/erp` es de un solo registro** (el propio taller activo), no un listado — `canCreate()=false`, `canDelete()=false`.

**Código nuevo:**
- `app/Http/Middleware/SetTallerActivo.php` — resuelve `session('taller_activo_id')` desde `asignacionesVigentes()`. No maneja el caso "cero talleres": `Filament\Http\Middleware\Authenticate` ya lo bloquea con 403 antes vía `canAccessPanel()` (mismo criterio), así que ese branch habría sido código muerto — se sacó tras confirmarlo con un test.
- `app/Http/Middleware/ForzarCambioPasswordMiddleware.php` — bloquea navegación si `debe_cambiar_password` o password expirada, registrado en ambos paneles con el id de panel como parámetro de ruta (`:admin`/`:erp`).
- `app/Filament/Concerns/InteractsWithCambioPassword.php` + `App\Filament\Admin\Pages\CambiarPassword` / `App\Filament\Erp\Pages\CambiarPassword` — resuelve el pendiente de `001` ("pantalla de cambio de contraseña obligatorio"). Rate limit propio `3/1min` (`WithRateLimiting`), reusa `CambiarPasswordAction` ya existente.
- `app/Filament/Erp/Widgets/TenantSwitcher.php` — solo visible si el usuario tiene >1 taller vigente.
- `AdminPanelProvider`/`ErpPanelProvider` personalizados: paleta Awesomic, middlewares nuevos.
- **002**: `App\Filament\Admin\Resources\PermisoResource` (catálogo, solo Super Admin), `App\Filament\Admin\Resources\RolResource` (roles globales, gate `admin.roles.gestionar`), `App\Filament\Erp\Resources\RolResource` (roles del taller activo, gates `roles.ver/crear/editar/eliminar`), `App\Filament\Erp\Resources\AsignacionRolResource` (formulario de asignación que invoca `AsignarRolAction` en vez de `Eloquent::create()` directo, gate `usuarios.gestionar`).
- **003**: `app/Actions/Talleres/CambiarEstadoTallerAction.php` (nueva), `App\Filament\Erp\Resources\TallerResource` (edición del taller activo, tabs datos/ubicación/categorías/horarios, `visible_en_mapa` fuera del form — se cambia con un Action dedicado que invoca `CambiarVisibilidadTallerAction`), `App\Filament\Admin\Resources\TallerResource` (listado global, acciones suspender/activar/cambiar propietario, soft delete con advertencia estática), `App\Filament\Admin\Resources\CategoriaResource` (catálogo simple).
- **32 tests Pest nuevos**: `tests/Feature/Sistema/SetTallerActivoMiddlewareTest.php`, `ForzarCambioPasswordTest.php`, `tests/Feature/Talleres/CambiarEstadoTallerActionTest.php`, `tests/Feature/Roles/ResourcesAutorizacionTest.php`, `tests/Feature/Talleres/ResourcesAutorizacionTest.php` — cubren autorización (con/sin permiso, cruce entre talleres) y smoke tests HTTP reales de cada página nueva (index/create/edit) para los 7 Resources.

### Bugs reales encontrados durante esta sesión (no solo de los tests)

1. **`->colors([... 'gray' => fn () => [...]])` rompía ambos paneles** con un 500 real (`array_map(): Argument #2 must be of type array, Closure given`) — el bug estaba en el propio `017-plan.md`, nunca se había ejecutado antes. Corregido a array plano.
2. **`Page::getUrl()` sin `panel:` explícito resuelve contra el panel `->default()` (admin) fuera de una request ya enrutada** — `ForzarCambioPasswordMiddleware` generaba la URL de `/admin/cambiar-password` incluso cuando corría dentro de `/erp`, porque ambas páginas comparten el mismo slug relativo `cambiar-password`. Corregido pasando `panel: $panelId` explícito en el middleware (y en los tests).
3. **Confirmado (no es bug, pero no obvio):** `tienePermiso()` no da acceso automático solo por tener el rol `super-admin` — el catálogo de permisos hay que adjuntarlo al rol explícitamente (`RolSistemaSeeder` sí lo hace en producción, `Permiso::pluck('slug')->all()`). Varios tests fallaron al principio por crear un rol `super-admin` "pelado" sin permisos adjuntos.

### Qué falta (fuera de alcance explícito de esta sesión)

- Rutas `routes/api.php`/`routes/web.php` de `017` — bloqueadas por controllers de `005`/`006`, que no existen todavía. No se tocan sin romper el orden estricto de `AGENTS.md`.
- CSS/tema Tailwind completo de `016-ui-design-system` (tokens, radii, tipografía) — se hace junto con `005`.
- Listener de recálculo de `calificacion_promedio` — depende de `Resena` (`006`).
- Conteo real de "entidades hijas activas" antes de soft-delete de un taller — depende de modelos de `007+`; el modal de advertencia por ahora tiene texto estático.
- Verificación visual en navegador real: se hizo un smoke test con `php artisan serve` + `curl` contra la BD de desarrollo (confirmando 200 en `/admin/login` y `/erp/login`, y que la paleta Awesomic se aplica — aparece `oklch(...)` en el HTML, no el azul/ámbar default), pero **no hubo un click-through completo en un navegador real** (`claude-in-chrome` no está disponible en esta sesión). Los 107 tests Pest sí ejercitan un render HTTP completo (200 OK) de cada página nueva en ambos paneles.
- ~~001/002/003 pueden marcarse `status: implemented`~~ **Hecho**: el usuario lo confirmó, `spec.md` de las tres features actualizado (`status: implemented`) el mismo día, después de esta sesión.

## Qué se hizo el 2026-07-26 (cuarta sesión): 003-gestion-talleres (backend completo + 28 tests verdes, total 75/75)

Siguiendo el orden estricto de `AGENTS.md` (paso 3, después de 001/002). **Código sin commitear todavía.**

- **Migración de reemplazo de `talleres`** (`2026_07_26_060001`): el prototipo original (`2026_07_19_174015`) no tenía slug/estado/visible_en_mapa/calificación y sobraba la columna `horario`. Se resolvió la brecha estructural que ya estaba documentada aquí: `roles.taller_id` y `asignaciones_rol.taller_id` (002) tenían FK hacia la tabla vieja — la migración suelta esas dos FK antes del `DROP`, recrea `talleres` con el esquema completo de `003-plan.md` (CHECK lat/lon/estado, índices GIST/estado/visible, soft delete), y vuelve a crear las FK contra la tabla nueva. Verificado corriendo `php artisan migrate` contra la BD real (no solo en el test DB): no había filas con `taller_id` no nulo en `roles`/`asignaciones_rol` todavía, así que no hubo huérfanos.
- **3 migraciones nuevas**: `categorias` (catálogo, slug único, `activo`), `talleres_categorias` (pivote PK compuesta con `orden`), `talleres_horarios` (`UNIQUE(taller_id, dia_semana)` + CHECK `dia_semana BETWEEN 1 AND 7` + CHECK `cerrado OR (apertura/cierre NOT NULL)` + CHECK `cierre > apertura`).
- **Bug real encontrado y corregido en infraestructura de `017`** (no de `003`, pero solo se manifestaba al usarla de verdad por primera vez): `HasGeolocation::bootHasGeolocation()` asignaba `$model->geom = DB::raw(...)` directamente. Como `Taller` ahora sí tiene `'geom' => GeometryCast::class` en sus casts, Eloquent intercepta esa asignación por `isClassCastable()` y llama a `GeometryCast::set()` con el `Expression` crudo en vez de un array `lat`/`lon` — hubiera roto con `Undefined property` en cualquier guardado. Corregido: el hook ahora asigna `['lat' => .., 'lon' => ..]` y deja que el cast arme el `ST_SetSRID(...)`. Nadie lo había detectado porque hasta ahora ningún modelo combinaba el trait con el cast en la misma columna.
- **Otro hallazgo (no bug, comportamiento esperado de Postgres pero no obvio)**: tras un `create()`, Postgres solo devuelve el PK en el INSERT — las columnas con `DEFAULT` a nivel de BD (`cantidad_resenas`, `calificacion_promedio`, y `geom` vía el cast) quedan en `null`/sin resolver en la instancia en memoria hasta hacer `->fresh()`/`->refresh()`. Esto llevó a simplificar `CambiarVisibilidadTallerAction`: la validación de "necesita geolocalización" se comprobaba también contra `$taller->geom`, pero en una instancia recién creada eso siempre da `null` (el cast recibe el `Expression` sin resolver, no un hex string). Se corrigió para validar solo `lat`/`lon` — son la fuente de verdad (constitution.md §1) y son `NOT NULL`, así que `geom` es redundante como validación.
- **Modelos**: `Taller` (usa `HasGeolocation`, `HasSlug` de spatie/laravel-sluggable, `SoftDeletes`; `scopeVisibleEnMarketplace()` con las 3 condiciones del spec — `SoftDeletes` ya excluye `deleted_at` por defecto), `Categoria` (`HasSlug`), `TallerHorario`.
- **`BelongsToTallerScope` actualizado**: ahora excluye automáticamente cualquier registro cuyo `taller_id` apunte a un taller con `deleted_at NOT NULL` (criterio explícito de `003`), usando `whereNotExists` correlacionado — **no** `whereNotIn` (`NULL NOT IN (...)` evalúa `NULL` en SQL y hubiera excluido también las filas con `taller_id` NULL, como los roles/asignaciones globales de `002`). Testeado aplicando el scope directamente contra la tabla `roles` (aún no existe ningún modelo real que use el trait `BelongsToTaller`; eso llega recién en `007+`), incluyendo el caso de restauración (`taller->restore()` reactiva el acceso sin migración).
- **`GuardarHorarioTallerRequest`**: Form Request con las mismas reglas que los CHECK de BD (apertura/cierre obligatorias si no está cerrado, cierre > apertura, único por taller+día), para dar error legible antes de tocar la BD. Sin controlador/ruta todavía (no hay UI, mismo patrón que el resto del proyecto) — testeado instanciando el Request directamente y corriendo `Validator::make()` con sus `rules()`.
- **Actions `CambiarVisibilidadTallerAction`/`CambiarPropietarioTallerAction`**: auditadas vía el helper `activity()` de spatie/laravel-activitylog (la misma tabla genérica que ya usaba `BelongsToTaller::sinScope()`), **no** la tabla `auditoria_eventos` de `015-auditoria` — esa tabla no existe todavía (015 depende de 003 y se implementa después en el orden del proyecto). Documentado en el docblock de ambas Actions para revisar cuando se implemente 015. Propietario valida que el nuevo dueño ya tenga una asignación de rol `OWNER` vigente en ese taller (no la crea automáticamente — responsabilidad de `AsignarRolAction`, ya existente).
- **Factories**: `TallerFactory` reescrita para el esquema nuevo (con estados `visible()`/`suspendido()`/`inactivo()`), `CategoriaFactory`, `TallerHorarioFactory` nuevas.
- **Colateral arreglado** (rompía por el cambio de esquema, no estaba en el alcance de `003` pero quedaría roto si no se tocaba): `TalleresSeeder` (importador de GeoJSON real de OSM, no registrado en `DatabaseSeeder`) ya no asigna `horario`/`geom` explícito — `geom` ahora se deriva solo. `TallerController@index` (ruta prototipo `GET /api/talleres`, previa a los specs) ya no selecciona la columna `horario` eliminada.
- **28 tests Pest nuevos** en `tests/Feature/Talleres/` cubriendo los criterios de `003-gestion-talleres/tasks.md`: CHECK lat/lon (BD), sincronización geom↔lat/lon, slug con colisión (sufijo numérico automático), visibilidad en marketplace (estado+visible+geom, excluye soft-deleted), CHECK de horario (cierre>apertura, obligatoriedad si no cerrado, único por día) tanto a nivel BD como Form Request, defaults de calificación en 0, `BelongsToTallerScope` excluye/reactiva hijas de taller soft-deleteado/restaurado, `CambiarVisibilidadTallerAction` y `CambiarPropietarioTallerAction` (permisos, vigencia de rol OWNER). **75/75 tests verdes en total** (47 previos + 28 nuevos), `laravel/pint` sin pendientes.

### Qué falta de `003` (por qué sigue en `status: draft`)

Mismo patrón que `001`/`002`: backend completo y probado, **UI pendiente**.

- Filament Resource `TallerResource` (panel `/erp`, filtrado por permiso `taller.configurar`) y vista global en `/admin` (tabla de todos los talleres para Super Admin).
- Listener de recálculo de `calificacion_promedio`/`cantidad_resenas` sobre eventos de `Resena`: **no se puede implementar todavía** porque el modelo `Resena` no existe (`006-resenas-favoritos` va después en el orden). Las columnas y sus defaults (0) ya están listas; el listener se agrega en `006`.
- Advertencia al Super Admin antes de soft-deletear un taller con entidades hijas activas: depende de (a) la UI de Filament y (b) que existan entidades hijas reales (`007` en adelante). El soft delete en sí (no bloqueante) ya funciona — falta el aviso visual.
- Ningún modelo real usa todavía el trait `BelongsToTaller` (los primeros candidatos son `Cliente`/`Vehiculo`/`Empleado` de `007`/`008`) — el scope se testeó aislado contra `roles` como vehículo de prueba, pero su primer uso real en producción llega recién ahí.

## Qué se hizo el 2026-07-26 (tercera sesión): 002-roles-permisos (backend completo + 21 tests verdes)

**Decisión de arquitectura confirmada con el usuario:** `002-roles-permisos/plan.md` dejaba abierto cómo mapear roles/permisos (usar Spatie nativo vs. tablas custom). Se eligió **implementación 100% custom, sin `spatie/laravel-permission`**: los criterios de aceptación exigen columnas de negocio en la asignación (`vigente_desde`/`vigente_hasta`, `asignado_por_usuario_sistema_id`, `activo` por asignación) que el pivot nativo de Spatie no soporta sin duplicar lógica en una tabla paralela.

- **`spatie/laravel-permission` fue removido por completo**: rollback de sus migraciones (batch 3), `composer remove`, `config/permission.php` borrado. No queda ningún rastro del paquete en el proyecto — si en el futuro alguien ve referencias a él en un `plan.md` viejo, están obsoletas.
- **4 migraciones nuevas** (`2026_07_26_050001` a `050004`): `roles` (taller_id NULL=global, unique parcial de slug para roles globales), `permisos` (catálogo global, sin taller_id), `roles_permisos` (pivote PK compuesta), `asignaciones_rol` (con `vigente_desde`/`vigente_hasta` + CHECK de que hasta >= desde, `activo`, `asignado_por_usuario_sistema_id`, unique parcial para asignaciones globales). Todas verificadas contra Postgres real (el CHECK de vigencia se probó con un insert que lo viola).
- **Modelos nuevos**: `App\Models\Rol` (belongsToMany Permiso vía `roles_permisos`, belongsTo Taller nullable), `App\Models\Permiso`, `App\Models\AsignacionRol` (con `estaVigente()`).
- **`UsuarioSistema` extendido**: `asignacionesRol()`, `asignacionesVigentes()` (filtra por `activo` + rango de fechas), `tienePermiso($slug, $tallerId=null)`, `esSuperAdmin()` (chequea rol slug `super-admin`). **`canAccessPanel()` ya no es un stub**: `/admin` exige `esSuperAdmin()`, `/erp` exige al menos una asignación vigente con `taller_id` no nulo.
- **`App\Actions\Roles\AsignarRolAction`**: valida usuario/rol activos, `rol.taller_id === asignacion.taller_id` (cuando el rol no es global), solo super admin asigna `SUPER_ADMIN` o roles globales, un owner/admin de taller no puede asignar fuera de su propio taller, rechaza asignaciones duplicadas. `asignadoPor = null` está reservado para el bootstrap del seeder (no existe super admin todavía cuando se crea el primero).
- **Seeders** (`PermisoSeeder`, `RolSistemaSeeder`, `SuperAdminSeeder`, cableados en `DatabaseSeeder`): 64 permisos del catálogo, 9 roles de sistema (`super-admin`, `owner`, `shop-admin`, `mecanico`, `cajero`, `recepcionista`, `supervisor`, `vendedor`, `marketplace-user`) con su mapping completo de permisos (`specs/017-infraestructura-sistema/plan.md` §8), y el primer usuario Super Admin (reutiliza `GenerarCredencialInicialAction` de 001 + `AsignarRolAction`). Corrido contra la BD real: 64 permisos, 9 roles, `esSuperAdmin()`/`tienePermiso()`/`canAccessPanel()` verificados con tinker.
- **Factories nuevas**: `TallerFactory`, `RolFactory`, `PermisoFactory` (los tres modelos ahora usan `HasFactory`).
- **21 tests Pest nuevos** en `tests/Feature/Roles/` cubriendo los 8 criterios de `002-roles-permisos/spec.md`: ámbito de taller (coincide/no coincide), duplicados, usuario/rol inactivo, solo super admin asigna SUPER_ADMIN, owner no asigna fuera de su taller ni roles globales, vigencia (CHECK de BD + `tienePermiso()` ignora asignaciones fuera de rango), acceso a paneles Filament, catálogo de permisos global (sin columna `taller_id`), slug de rol global único pero repetible entre talleres distintos. **47/47 tests verdes en total** (26 de 001 + 21 de 002), `laravel/pint` sin pendientes.
- Se actualizaron los comentarios obsoletos en `GenerarCredencialInicialAction`/`CrearUsuarioSistemaAction` (de 001) que decían "pendiente hasta que exista 002" — ahora documentan que la asignación de rol se compone desde afuera con `AsignarRolAction`, a propósito (responsabilidad única).

### Qué falta de `002` (por qué sigue en `status: draft`)

Igual que `001`: backend completo y probado, **UI pendiente** — no se marca `implemented` hasta que exista también la interfaz.

- Filament Resource para que el Super Admin administre el catálogo de permisos y los roles globales (`plan.md` mencionaba `filament/spatie-laravel-permission-plugin`, descartado junto con Spatie — hay que construir un Resource propio).
- UI para que un owner/admin de taller cree roles personalizados de su taller y les asigne permisos.
- Formulario de asignación de rol filtrado por taller activo (dependía del plugin de Spatie, ahora es un Resource/Action propio sobre `AsignarRolAction`).
- No hay ninguna restricción de código que impida crear un `Permiso` fuera del seeder (el criterio "solo el super admin crea/edita el catálogo de permisos" no tiene ninguna superficie de escritura expuesta todavía, así que no aplica un enforcement real hasta que exista esa UI).

### Dos brechas estructurales detectadas (documentadas, no bloquean 002 pero hay que resolverlas cuando toque)

1. **`MARKETPLACE_USER` no se puede asignar realmente**: `asignaciones_rol.usuario_sistema_id` es FK a `usuarios_sistema`, no a `usuarios_marketplace`. El rol se seedea igual (queda en el catálogo, `spec.md` lo pide explícitamente como "rol de sistema recomendado"), pero no hay forma de asignarlo a un usuario marketplace con el esquema actual. Cuando se implemente `006-resenas-favoritos`, decidir: (a) los permisos `marketplace.*` se chequean directamente sin pasar por `asignaciones_rol` (más simple, probablemente lo correcto ya que el guard es distinto), o (b) se extiende el esquema. **Recomendación: opción (a)**, no forzar el sistema de roles del guard `sistema` sobre el guard `web`.
2. **FK de `roles`/`asignaciones_rol` hacia `talleres.id`**: apunta al prototipo actual de `talleres` (`2026_07_19_174015_create_talleres_table.php`). Cuando `003-gestion-talleres` ejecute su migración de reemplazo (`DROP TABLE talleres` + recrear, ya documentada en `017-plan.md` §7), esa `DROP` va a fallar por las FK de `roles.taller_id` y `asignaciones_rol.taller_id` apuntando a la tabla vieja. Quien implemente `003` necesita o (a) dropear/recrear esas FK como parte de la misma migración de reemplazo, o (b) usar `Schema::table('talleres')->drop()` con las FKs dependientes contempladas explícitamente. ~~**No se resolvió aquí** porque es trabajo de `003`, fuera de orden de implementación.~~ **Resuelto en la cuarta sesión** (opción (a): `2026_07_26_060001_replace_talleres_table.php` suelta ambas FK antes del DROP y las recrea después contra la tabla nueva).

## Qué se hizo el 2026-07-26 (segunda sesión): commit, correcciones post-instalación y push

- Se corrigieron `AGENTS.md`, `017-plan.md` y `017-tasks.md`:
  - **AGENTS.md**: reescrito con tabla de orden de implementación obligatorio (paso 0→7) + notas sobre specs transversales.
  - **017-plan.md**: `maatwebsite/laravel-excel:^3` → `maatwebsite/excel` (v4.x, compatible Laravel 11+; si falla, fallback `spatie/simple-excel`). Eliminado `@tailwindcss/forms` — **Tailwind v4 no lo necesita**, Preflight + utility classes manejan forms nativamente.
  - **017-tasks.md**: refleja los mismos cambios con nota de verificación.
- Se creó la rama `specs/planificacion`, se agregaron todos los specs y documentación, y se subió a `origin/specs/planificacion` (commit `7e7f074`).
- Posteriormente se commiteó y pusheó el código de implementación de `017` (infraestructura: paquetes, PostGIS, BelongsToTaller, GeometryCast, etc.) y `001` (backend + tests de identidad y autenticación) en el commit `eca323c`. **Ya no está pendiente — no reintentar.**

## Qué se hizo el 2026-07-26 (primera sesión): instalación de paquetes (`017-infraestructura-sistema`, fase 0)

Se instalaron los paquetes Composer/NPM de `017-infraestructura-sistema`. El proyecto Laravel ya no es un esqueleto vacío: corre Laravel 13.8 / PHP 8.5.4, y sobre eso se instalaron Filament v5.7.3, spatie/laravel-permission 7.4.2, laravel/socialite 5.29, spatie/laravel-sluggable 4.0.2, spatie/laravel-activitylog 5.0, spatie/simple-excel 3.10, guzzlehttp/guzzle 7, y (dev) laravel/breeze 2.4, barryvdh/laravel-debugbar 4.4. Pest v4 ya venía instalado con el skeleton.

**Todo esto se verificó con Context7 + Packagist contra el estado real de PHP 8.5.4/Laravel 13.8 antes de instalar**, porque `017-plan.md` había quedado desactualizado en varios puntos. El detalle completo (qué cambió y por qué) está documentado en `specs/017-infraestructura-sistema/plan.md` §1 y `tasks.md` — léelos antes de asumir que el plan original es fiel a lo instalado. Resumen de las decisiones que importan para retomar:

1. **Filament v5, no v3.2** (decisión confirmada con el usuario). v3 no tiene compatibilidad garantizada con Laravel 13/PHP 8.5 y plugins clave ya no la soportan.
2. **`maatwebsite/excel` no se pudo instalar** (conflicto de versión de PHP con `phpoffice/phpspreadsheet`). Se sustituyó por `spatie/simple-excel` + `pxlrbt/filament-excel` fue removido del plan. **Los exports en Resources de Filament (specs 007-015) se implementan con acciones custom sobre `SimpleExcelWriter`, no con `ExportBulkAction`.**
3. **Se detectaron y corrigieron dos APIs de Filament inventadas** en el plan original (`->tenantOwnership(Ownership::new(...))` y `->spatiePermission()` — ninguna existe en ninguna versión real de Filament). La autorización real se integra vía Laravel Model Policies; la tenancy nativa real es `->tenant(Model::class, slugAttribute: ...)`. **Al implementar los PanelProviders y cualquier Resource de Filament, no confiar en la sintaxis literal de versiones previas de `017-plan.md` — verificar contra la documentación real de Filament v5 (Context7) primero.**
4. Se habilitó la extensión PHP `intl` en `php.ini` (requerida por Filament v5, estaba deshabilitada en el sistema).
5. `php artisan filament:install --panels` + `make:filament-panel erp` ejecutados: `AdminPanelProvider` y `ErpPanelProvider` creados y registrados en `bootstrap/providers.php`. **Aún no tienen la configuración de colores/middleware/tenancy del plan** — son el scaffold default de Filament, falta personalizarlos.
 6. Config y migraciones de `spatie/laravel-permission` y `spatie/laravel-activitylog` publicadas (tablas aún no migradas — falta correr `php artisan migrate` una vez esté lista la migración de PostGIS que debe ir primero).

### Qué más se hizo el 2026-07-26: resto de infra autocontenida de `017`

Después de la instalación de paquetes se completó todo lo de `017-infraestructura-sistema` que **no depende de modelos de otras specs**:
- Migración PostGIS (`0000_00_00_000001_create_extension_postgis.php`), migrada. PostgreSQL 18.3 + PostGIS 3.6.2 ya estaban en el servidor.
- `config/app.php` timezone `America/La_Paz`.
- Queue: `QUEUE_CONNECTION=database` ya estaba en `.env`, y la tabla `jobs` ya viene por defecto en el skeleton de Laravel 13 — no hizo falta nada.
- `config/permission.php`: `teams=true` + `team_foreign_key=taller_id`, seteado **antes** de migrar las tablas de permisos (importante: si se migra con `teams=false` y se cambia después, hay que dropear y re-migrar esas tablas — spatie/laravel-permission no las actualiza retroactivamente).
- `app/Traits/BelongsToTaller.php` + `app/Models/Scopes/BelongsToTallerScope.php` + `sinScope()`.
- `app/Actions/SequentialCodeGenerator.php` (con un bug del plan corregido: el parámetro `$retries` no se usaba).
- `app/Traits/HasGeolocation.php` + `app/Casts/GeometryCast.php` — **verificados contra PostGIS real** (round-trip `get()`/`set()` con `ST_AsHexEWKB` da el resultado exacto, no solo revisado a ojo). El plan original no traía código para `GeometryCast`, se implementó desde cero decodificando EWKB hexadecimal a mano.

### Por qué se pausó `017` ahí y se sigue con `001`

Lo que queda de `017-infraestructura-sistema` (seeding de permisos/roles/super admin/catálogos, personalización real de los PanelProviders, middleware `SetTallerActivo`, rutas API/web, migración de reemplazo de `talleres`) **depende de modelos que no existen hasta `001`/`002`**: `usuarios_sistema`, `Identidad`, `Rol`/`Permiso` con su relación `asignacionesRol()`, controladores de `005`/`006` para las rutas. Esto es una inconsistencia estructural del plan original de `017` (fue escrito asumiendo que esos modelos ya existirían), documentada en detalle en `specs/017-infraestructura-sistema/tasks.md`.

**Decisión (2026-07-26, confirmada con el usuario):** en vez de forzar un workaround (ej. crear la migración de reemplazo de `talleres` sin la FK a `usuarios_sistema`, o inventar el shape de `asignacionesRol()`), se pausa `017` en su punto autocontenido y se pasa a implementar `001-identidad-autenticacion`. Cuando existan esos modelos, retomar `017-infraestructura-sistema/tasks.md` para terminar: seeding, `SetTallerActivo`, paneles Filament personalizados (paleta, middleware, guard `sistema`), rutas, y la migración de reemplazo de `talleres` (con la FK correcta esta vez).

## Qué se hizo después: 001-identidad-autenticacion (backend completo, sin UI)

Implementado y **verificado funcionalmente contra la BD real** (transacciones con rollback en cada prueba, no solo `class_exists`):

- 6 migraciones (`identidades`, `usuarios_marketplace`, `identidades_oauth`, `usuarios_sistema`, `credenciales_sistema`, `historial_passwords`) + 6 modelos Eloquent con relaciones probadas de punta a punta.
- Se eliminó la tabla/modelo `users` default de Laravel (no es parte de esta arquitectura) — `DatabaseSeeder` limpiado.
- Guards `web`/`sistema` en `config/auth.php`. `UsuarioMarketplace`/`UsuarioSistema` extienden `Illuminate\Foundation\Auth\User`, sin remember-me (`$rememberTokenName = ''`).
- OAuth Google: `GoogleAuthController`, `LoginOrRegisterMarketplaceUserAction` (idempotencia por `provider_subject` verificada: 2 logins seguidos no duplican nada).
- `App\Auth\UsuarioSistemaProvider` (provider custom): rechaza login si `activo=false` o `bloqueado_hasta` en el futuro — un provider `eloquent` estándar no puede validarlo porque esos datos viven en `CredencialSistema`, no en `UsuarioSistema`. Probado: cuenta bloqueada con password correcta → rechazada; desbloqueada → aceptada.
- Listeners `RegistrarIntentoFallidoListener`/`ReiniciarIntentosFallidosListener` (auto-descubiertos, sin registro manual): 5 fallos → bloqueo 15 min; login exitoso resetea el contador. Probado end-to-end con `Auth::attempt()` real.
- `CambiarPasswordAction`: rechaza reutilizar la actual o cualquiera de las últimas 5, recorta el historial a 5. Probado con 7 cambios sucesivos.
- `GenerarCredencialInicialAction` / `CrearUsuarioSistemaAction`: generan password temporal de 16 caracteres que cumple la política (`Password::min(12)->mixedCase()->numbers()->symbols()`), verificado contra el validador real.
- `App\Exceptions\BusinessException` (transversal, constitution.md §4) creada porque hacía falta para `CambiarPasswordAction`.
- `CheckSessionExpiration` (30 min de inactividad, guard `sistema`) registrado en ambos PanelProviders de Filament.
- Hallazgo importante: Filament v5 **ya trae rate limiting nativo** en su página de Login (`WithRateLimiting`, 5 intentos/60s por IP) — cubre el `throttle:5,1` del spec sin código adicional.
- Decisión: **no se corrió `laravel/breeze:install`** — genera scaffolding de login/registro con contraseña atado a una tabla `users` que ya no existe en esta arquitectura. Se implementó el flujo OAuth a mano en su lugar. El paquete queda instalado por si sirve de referencia.

### Qué falta de `001`

- **UI**: pantalla/Livewire component de cambio de contraseña obligatorio (primer login + expirada) — el backend (`CambiarPasswordAction`) ya está listo para que la consuma.
- OWASP: `APP_DEBUG=false`/`composer audit` en CI (no hay pipeline de CI configurado todavía en este repo), signed routes para desbloqueo de usuario (no hay pantalla que las emita aún, es de `002`/`008`).
- La asignación de rol `OWNER`/rol de empleado al crear un usuario sistema queda con una nota explícita en el código (`GenerarCredencialInicialAction`, `CrearUsuarioSistemaAction`) — depende del catálogo de roles de `002-roles-permisos`, que no existe todavía.

## Qué se hizo después: suite de tests Pest de 001 (26/26 verdes)

Se provisionó infraestructura de test real (no se podía usar antes de ahora):
- BD dedicada `taller_test` en el PostgreSQL local, `.env.testing`, `RefreshDatabase` habilitado en `tests/Pest.php`.
- **`phpunit.xml` forzaba SQLite en memoria** (`DB_CONNECTION=sqlite`, `DB_DATABASE=:memory:`), lo que tiene prioridad sobre `.env.testing` y violaba `constitution.md` §6 explícitamente. Se quitaron esas líneas.
- Factories nuevas: `IdentidadFactory`, `UsuarioSistemaFactory`, `CredencialSistemaFactory`, `UsuarioMarketplaceFactory`.
- 9 archivos de test en `tests/Feature/Identidad/` cubriendo los 9 criterios de `001-identidad-autenticacion/tasks.md` (idempotencia OAuth, separación de guards, bloqueo por intentos, rate limiting nativo de Filament vía `Livewire::test()`, política de password incluyendo `uncompromised()` con `Http::fake()`, expiración de password, reutilización de historial, expiración de sesión, password nunca expuesto en serialización).
- **26/26 tests pasan**, 55 assertions. `laravel/pint` sin pendientes.

### Tres bugs reales que la suite encontró (no solo bugs de los tests) — habrían llegado a producción

1. **`UsuarioSistema` no implementaba `Filament\Models\Contracts\FilamentUser`** → Filament v5 deniega el acceso a **todos** los paneles (403) por defecto sin ese contrato. Ningún usuario sistema habría podido entrar a `/admin` ni `/erp`. Corregido con `canAccessPanel()` (por ahora solo exige `activo`; restricción real por rol pendiente de `002`).
2. **Tampoco implementaba `Filament\Models\Contracts\HasName`** → Filament intenta leer un atributo `name` inexistente (el modelo usa `nombre`/`apellido`) y explota con `TypeError` al renderizar cualquier página. Corregido con `getFilamentName()`.
3. **Bug real en `CheckSessionExpiration`**: `now()->diffInMinutes($ultimaActividad) > 30` — en la versión de Carbon de este proyecto `diffInMinutes()` devuelve un valor **con signo** (negativo para fechas pasadas), no absoluto. La sesión nunca habría expirado por inactividad pese a que el código "se veía" correcto. Corregido comparando fechas directamente (`->lt(now()->subMinutes(30))`).

Estos tres son la razón de peso para no saltarse la suite de tests aunque el backend ya estuviera "funcionalmente verificado a mano" — las pruebas manuales por tinker no pasan por la pila real de Filament (paneles, Livewire) ni ejercitan el paso del tiempo, así que no los habrían atrapado.

## Qué es este proyecto

Plataforma de talleres mecánicos en Santa Cruz, Bolivia: marketplace público (conductores buscan talleres, con reseñas/favoritos, login Google) + mini-ERP por taller (clientes, vehículos, empleados, inventario, órdenes de trabajo, notas de venta, pagos), multi-tenant por `taller_id`. Stack objetivo: Laravel 13, PHP 8.4+, PostgreSQL + PostGIS, Blade+Tailwind+Alpine (marketplace), FilamentPHP v3 (ERP + Super Admin).

## Qué se hizo en esta sesión

Se ejecutó una reestructuración completa de `/specs`, que originalmente eran 4 archivos en bruto sin frontmatter, mezclando QUÉ y CÓMO, sin descomponer por feature:
- `MacroSpec.md` (monolito de ~17 dominios de negocio)
- `Baterias.md` (decisiones de paquetes Composer)
- `UI_Spec.md` (pantallas/UI de los 3 productos)
- `DataBase/database.md` (39 tablas del esquema físico)

**Esos 4 archivos ya no existen** — su contenido se migró y se eliminaron (nunca estuvieron en git, no se perdió historial).

### Resultado

```
AGENTS.md                    <- guía de navegación para agentes (leer primero)
memory/constitution.md       <- convenciones no-negociables (arquitectura, datos, reglas transversales, errores, UI, testing, seguridad)
resume.md                    <- este archivo
specs/
  001-identidad-autenticacion/
  002-roles-permisos/
  003-gestion-talleres/
  004-solicitud-alta-taller/
  005-marketplace-busqueda-perfil/
  006-resenas-favoritos/
  007-clientes-vehiculos/
  008-empleados-usuarios-erp/
  009-catalogo-servicios/
  010-inventario-repuestos/
  011-ordenes-trabajo/
  012-notas-venta/
   013-pagos/
   014-notificaciones/
   015-auditoria/
   016-ui-design-system/
   017-infraestructura-sistema/
```

Cada carpeta de feature tiene:
- `spec.md` — QUÉ (frontmatter YAML `id`/`status`/`depends_on`/`resumen` + criterios de aceptación Given/When/Then). Sin detalles de implementación.
- `plan.md` — CÓMO (tablas, columnas, constraints, modelos Eloquent, Actions, paquetes, Filament Resources).
- `tasks.md` — checklist de tareas atómicas y verificables derivadas del plan.

Las 17 features están numeradas por orden de dependencia (`depends_on` en el frontmatter). **Todas están en `status: draft`** — nada de esto está implementado todavía en código (ver más abajo).

## Estado real del código

El proyecto Laravel ya no es un esqueleto:
- **Stack instalado**: Filament v5.7.3, laravel/socialite 5.29, spatie/laravel-sluggable 4.0.2, spatie/laravel-activitylog 5.0, spatie/simple-excel 3.10, guzzlehttp/guzzle 7, laravel/breeze 2.4, barryvdh/laravel-debugbar 4.4 (dev). Pest v4 viene con el skeleton. **`spatie/laravel-permission` fue instalado y luego removido** (ver sesión 2026-07-26 tercera) — roles/permisos son 100% custom, no depende de ese paquete.
- **017-infraestructura-sistema**: migración PostGIS ejecutada, timezone configurado, `BelongsToTaller` trait + scope, `HasGeolocation` trait + `GeometryCast`, `SequentialCodeGenerator`. Seeding de permisos/roles/Super Admin hecho. **`SetTallerActivo` + `ForzarCambioPasswordMiddleware` + paneles Filament personalizados (paleta Awesomic) — hecho en la quinta sesión.** Pendiente: rutas API/web (bloqueadas por `005`/`006`), CSS/tema Tailwind completo (junto con `016`+`005`).
- **001-identidad-autenticacion**: backend (6 migraciones + modelos, OAuth Google, guards `web`/`sistema`, provider custom, bloqueo por 5 intentos, expiración de sesión 30 min, historial de 5 contraseñas) + **UI de cambio de contraseña obligatorio (quinta sesión)**. 26 tests de backend + cubierto por los tests de middleware de la quinta sesión.
- **002-roles-permisos**: backend (4 migraciones + modelos `Rol`/`Permiso`/`AsignacionRol`, `AsignarRolAction`, `tienePermiso()`/`esSuperAdmin()`/`canAccessPanel()`, catálogo de 64 permisos + 9 roles de sistema seedeados) + **UI (quinta sesión): `PermisoResource`, `RolResource` (admin+erp), `AsignacionRolResource`**. 21 tests de backend + 12 tests de autorización/render de Resources.
- **003-gestion-talleres**: backend (migración de reemplazo de `talleres`, `categorias`/`talleres_categorias`/`talleres_horarios`, modelos `Taller`/`Categoria`/`TallerHorario`, `CambiarVisibilidadTallerAction`/`CambiarPropietarioTallerAction`) + **nueva Action `CambiarEstadoTallerAction` y UI (quinta sesión): `TallerResource` (erp+admin), `CategoriaResource`**. 28 tests de backend + 12 tests de `CambiarEstadoTallerAction`/autorización de Resources. **Listener de recálculo de `calificacion_promedio`/`cantidad_resenas` implementado en la octava sesión** (`006-resenas-favoritos`). Pendiente: conteo real de hijas activas antes de soft-delete (depende de `007+`).
- **004-solicitud-alta-taller** (sexta sesión): completo — backend (`solicitudes_taller`/`solicitudes_taller_historial`, modelos, 6 Actions de máquina de estados con `lockForUpdate`), UI pública (`/solicitudes-taller/nueva`, formulario multi-paso; `/solicitudes-taller/{token}`, seguimiento) y Filament (`SolicitudTallerResource` en `/admin`). 44 tests. Pendiente a propósito: detección de duplicados (criterio opcional del spec, no activado). **UI pública todavía en el layout mínimo pre-016 (`layouts/marketplace.blade.php`), no se migró al design system Awesomic — queda pendiente para una sesión de pulido.**
- **005-marketplace-busqueda-perfil + 016-ui-design-system** (séptima sesión): completos — tokens Awesomic + temas Filament v5 (`->viteTheme()`+`->font()`), Alpine.js instalado (no lo estaba), layouts/componentes Blade del marketplace, `GET /` (home), `GET /talleres/buscar`, `GET /talleres/{slug}` (perfil, 404 si no público), `GET /api/talleres/search` (JSON, `throttle:30,1`). Dos bugs reales corregidos en `HasGeolocation` (`scopeConDistanciaA` perdía columnas del modelo, `scopeCercanoA` no filtraba nada por usar `ST_DWithin` en grados en vez de metros — ver detalle arriba). 21 tests nuevos. Pendiente a propósito: verificación visual en navegador real.
- **006-resenas-favoritos** (octava sesión): completo — backend (`resenas`/`favoritos`, modelos, eventos `ResenaGuardada`/`ResenaEliminada` + listener síncrono de recálculo, Actions `GuardarResenaAction`/`EliminarResenaAction`/`ModerarResenaAction`), API (`POST /api/resenas`, `DELETE /api/resenas/{id}`, `POST /api/favoritos`, `POST /api/favoritos/delete`, guard `web`), UI pública (formulario de reseña + botón de favorito en el perfil, `GET /dashboard` con "Mis Favoritos"/"Mis Reseñas") y Filament (`ModeracionResenasResource` en `/admin`). Dos bugs reales corregidos (ver detalle arriba): `calificacion_promedio`/`cantidad_resenas` fuera de `$fillable` hacía que el recálculo se ignorara en silencio; no existía redirect de login para el guard `web` (`RouteNotFoundException` en vez de 302). 27 tests nuevos.
- **007-clientes-vehiculos** (novena sesión): completo — backend (`clientes`/`vehiculos`, modelos con mutators `Attribute::make()` para normalizar `nit_ci`/`placa`, `scopeActivos()` en ambos para consumo futuro de `011`/`012`), Form Requests (`ClienteRequest`/`VehiculoRequest`) y Filament (`ClienteResource`/`VehiculoResource` en `/erp`). Sin bugs de negocio nuevos, pero un riesgo real de falso-positivo en `scopedUnique()` sobre un campo opcional fue identificado leyendo el código de Filament y verificado con 3 tests Livewire reales (ver detalle arriba) antes de darlo por bueno. 42 tests nuevos, incluyendo los primeros tests `Livewire::test(...)->fillForm(...)->call('create')` del proyecto (hasta ahora solo se probaban páginas Filament con `assertSuccessful()` en la carga, no el guardado real del formulario).
- **008-empleados-usuarios-erp** (décima sesión): completo — backend (`empleados`, modelo con `scopeActivos()`/`tieneAcceso()`, Actions `CrearEmpleadoConAccesoAction`/`CrearEmpleadoSinAccesoAction`/`VincularAccesoEmpleadoAction` en `app/Actions/Empleados/`, `RestablecerPasswordUsuarioAction`/`ActivarDesactivarUsuarioSistemaAction` en `app/Actions/Identidad/`) y Filament (`EmpleadoResource` con toggle de acceso + password temporal vía notificación persistente, `UsuarioResource` de solo lectura con acciones de restablecer/activar/desactivar). Sin bugs de negocio nuevos; se extendió el criterio del `plan.md` para soportar "otorgar acceso después" a un empleado que ya existía sin acceso (no solo en la creación), reutilizando la misma Action interna. 29 tests nuevos.
- **009-catalogo-servicios** (undécima sesión): completo — backend (`servicios_catalogo`, modelo `ServicioCatalogo` con `scopeActivos()`, `ServicioCatalogoRequest`) y Filament (`ServicioResource` en `/erp`). Sin bugs de negocio nuevos. Pendiente a propósito: snapshot real de `precio_unitario` en líneas de orden/nota (depende de `011`/`012`, no existen todavía). 19 tests nuevos.
- **010-inventario-repuestos** (duodécima sesión): completo — backend (`repuestos`/`unidades_medida`/`proveedores`/`repuestos_proveedores`/`inventario_movimientos`, modelos con mutators de normalización `''`→`null`, `RegistrarMovimientoInventarioAction` con `lockForUpdate`/protección de doble salida, evento `StockBajoDetectado` sin listener) y Filament (`RepuestoResource` con acción "Ajustar Stock", `ProveedorResource` con la primera `RelationManager` N:M del proyecto, `MovimientoInventarioResource` de solo lectura, `UnidadMedidaResource` en `/admin` — extensión no listada en `plan.md` pero necesaria para que `unidad_medida_id` tenga datos). Bug real encontrado por tests: `Proveedor` sin `$table` explícito pluralizaba mal a "proveedors". 56 tests nuevos.
- **011-ordenes-trabajo** (decimotercera sesión): completo — backend (`ordenes_trabajo` + 2 tablas de líneas + historial + notas, máquina de estados de orden y de línea, `recalcularTotales()` transaccional, 7 Actions en `app/Actions/Ordenes/`, integración real con `010` vía `RegistrarMovimientoInventarioAction` para descuento/reposición de stock por línea de repuesto, FK real agregada a `inventario_movimientos.orden_trabajo_repuesto_id` que `010` había dejado pendiente) y Filament (`OrdenTrabajoResource` con creación vía Action, cambio de estado/anulación como acciones de cabecera dedicadas, 4 `RelationManagers` como pestañas, lista con tabs por estado en vez de Kanban — decisión documentada). Brecha de integración con `012` resuelta en la decimocuarta sesión. 67 tests nuevos, incluyendo los primeros tests Livewire directos sobre `RelationManagers` del proyecto.
- **012-notas-venta** (decimocuarta sesión): completo — backend (`notas_venta`/`notas_venta_lineas`, máquina de estados, 3 Actions `CrearNotaVentaDesdeOrdenAction`/`CrearNotaVentaDirectaAction`/`AnularNotaVentaAction`, descuento de stock en creación directa vía `010`, bloqueo de anulación si `monto_pagado > 0`) y Filament (`NotaVentaResource` con selector origen orden/directa, `Repeater` condicional, solo lectura en edición + acción anular). Hallazgo Filament v5 corregido: `->visible()` no excluye de validación. Integración real con `011`: `AnularOrdenTrabajoAction` bloquea/auto-anula notas de venta según estado. 48 tests nuevos + 6 tests extendidos de `011`. ~~Brecha a propósito: transiciones a `PENDIENTE`/`PAGADA` dependen de `013-pagos`.~~ **Resuelto en la decimoquinta sesión.**
- **013-pagos** (decimoquinta sesión): completo — backend (`metodos_pago`/`pagos`, sin `taller_id` propio en `pagos` — aislamiento vía `nota_venta_id`, sin soft delete — se anula cambiando `estado` —, `MetodoPagoSeeder` con los 4 métodos base), `NotaVenta::recalcularTotales()` (de `012`) extendido para sumar pagos `CONFIRMADO` y derivar `estado` (`PAGADA`/`PENDIENTE`/`EMITIDA`), 2 Actions `RegistrarPagoAction`/`AnularPagoAction` (`lockForUpdate` sobre la nota, `AnularPagoAction` auditada vía `activity()`) y Filament (`PagosRelationManager` en `NotaVentaResource`: modal "Registrar Pago" con `maxValue()`=saldo, acción "Anular pago", gateado explícitamente por `pagos.registrar`/`pagos.anular`). Decisión deliberada: sin `MetodoPagoResource` (el seeder ya deja datos usables, a diferencia de `UnidadMedidaResource` en `010`). 22 tests nuevos + 1 test de `012` actualizado (`NotaVentaTest.php`, ahora usa un `Pago` real en vez de fijar `monto_pagado` a mano).
- **014-notificaciones** (decimosexta sesión): completo — tabla `notificaciones` (destinatario mutuamente excluyente `usuario_marketplace_id`/`usuario_sistema_id`), `Notificacion` + `CrearNotificacionAction`/`MarcarNotificacionLeidaAction`, 10 clases en `app/Notifications/` conectadas a sus 6 features origen (listeners nuevos sobre 3 eventos ya existentes de `006`/`010`/`013`, primer uso de Observers del proyecto para `orden.*`/`nota.emitida`, llamada directa en las Actions de `solicitud.*`/`usuario.creado`, comando programado diario para `password.expirada`), endpoint `POST /notificaciones/{id}/marcar-leida` (guard `web,sistema` combinado), campana Livewire en el topbar de `/admin`+`/erp` vía `renderHook`, sección en el dashboard marketplace. 2 bugs reales de manejo de excepciones JSON/redirect corregidos (`AuthenticationException::redirectUsing()` + `shouldRenderJsonWhen` ampliado en `bootstrap/app.php`). 40 tests nuevos.
- **Total: 521/521 tests Pest verdes** (481 previos + 40 de `014`), `laravel/pint --dirty` sin pendientes, `npm run build` sin errores.
- **Prototipo Taller viejo**: ya reemplazado por la migración de `003` (`2026_07_26_060001_replace_talleres_table.php`). El `TalleresSeeder` (importador de GeoJSON de OSM, no registrado en `DatabaseSeeder`) se actualizó para no romper con el esquema nuevo. La ruta/controlador prototipo `GET /api/talleres` (`TallerController@index`) y `welcome.blade.php` se **eliminaron** en la séptima sesión, reemplazados por el home real y `GET /api/talleres/search`.
- Git: repositorio en rama `specs/planificacion`. Todo el código hasta `014-notificaciones` inclusive está commiteado y pusheado en `origin/specs/planificacion` (`eca323c` → `08c2d90` → `dee42c2` → `96bce0d` → `c42d59e` → `b31d74e` → `e8aed6c` → `b8802c3` → `a604bd3` → `4da1c16` → `c8a9bb5` → `a126403` → `76a1e7a` → `b7e59be` → `dbe231e` → `d5d8f14` → `e4d14fa` → `d83b268`).

## Decisiones resueltas (2026-07-25)

1. **`002-roles-permisos` — Roles operativos**: son roles de sistema fijos (`es_sistema = true`). El admin de taller puede crear roles personalizados adicionales pero no eliminar/modificar los de sistema.
2. **`002-roles-permisos` — Catálogo de permisos**: lista final aprobada (60 permisos en 16 módulos, definida en `plan.md`).
3. **`012-notas-venta` — Múltiples notas por orden**: permitido (regla permisiva), respetando ACID.
4. **`011-ordenes-trabajo` / `012-notas-venta` — Motivo de anulación**: **obligatorio** en ambos casos. Reglas ACID definidas: bloqueo si hay pagos, auto-anulación de nota EMITIDA al anular orden, reposición automática de stock.
5. **`016-ui-design-system`**: aprobado. Awesomic en marketplace (Blade), paleta mapeada en Filament. Sin modo oscuro en marketplace. Alpine stores + data components.

## Brechas críticas cubiertas (2026-07-25)

1. **`017-infraestructura-sistema`** — paquetes, paneles Filament, BelongsToTaller, concurrencia en códigos, rutas API, helpers PostGIS, migración de reemplazo de talleres.
2. **`005`** — endpoint JSON `GET /api/talleres/search` para Alpine.fetch().
3. **`006`** — endpoints `POST /api/resenas`, `DELETE /api/resenas/{id}`, `POST /api/favoritos`, `POST /api/favoritos/delete`.
4. **`001`** — ruta `POST /auth/logout` para marketplace.
5. **`003`** — política de soft delete en cascada (sin bloqueo, advertencia al Super Admin).
6. **`003`** — migración de reemplazo del prototipo `talleres` documentada en `017-plan.md`.

## Brechas de infraestructura y calidad cubiertas (2026-07-25)

| # | Omisión | Solución |
|---|---|---|
| 1 | Seeding inicial | `017-plan.md` §8: Super Admin, permisos, roles + mapping, métodos pago, unidades, categorías |
| 2 | PostGIS requisito | `017-plan.md` §0: migración `CREATE EXTENSION postgis` |
| 3 | Timezone | `017-plan.md` §0: `config/app.php: timezone => America/La_Paz` |
| 4 | Queue | `017-plan.md` §0: `QUEUE_CONNECTION=database`, tabla jobs migrada, recálculo síncrono |
| 5 | "Abierto ahora" vía SQL | `005-plan.md`: LEFT JOIN a `talleres_horarios` con `whereTime`, no post-query |
| 6 | Soft delete bloqueante | `constitution.md` §3.13: bloquea si hay hijos activos (excepción: taller) |
| 7 | Error handling pattern | `constitution.md` §4: `BusinessException` → 422/redirect, toast Filament, JSON error |
| 8 | Autocomplete en ERP | `017-plan.md` §9: patrón `->searchable()->getSearchResultsUsing()` con global scope |
| 9 | File uploads | `constitution.md` §1: `Storage::putFile()` (disco `public`), no medialibrary |
| 10 | Naming convention | `constitution.md` §1: `app/Actions/{Feature}/{Nombre}Action.php` |
| 11 | Test DB | `constitution.md` §6: PostgreSQL dedicado (`.env.testing`), no SQLite |
| 12 | Exportaciones | `017-plan.md` §9: Resources que exportan listados |
| 13 | Loading states | `constitution.md` §5: skeleton en búsqueda, spinner en mapa |
| 14 | Notification triggers | `014-plan.md`: tabla con 10 eventos, feature origen, destinatario |

## Próximos pasos sugeridos

1. ~~Commitear y pushear el código de implementación de `017` y `001`~~ **Hecho** (commit `eca323c` en `origin/specs/planificacion`).
2. ~~Implementar `002-roles-permisos`~~ **Hecho** (commit `08c2d90` en `origin/specs/planificacion`).
3. ~~Implementar `003-gestion-talleres`~~ **Hecho** (commit `dee42c2` en `origin/specs/planificacion`).
4. ~~Retomar `017-infraestructura-sistema`: `SetTallerActivo`, paneles Filament personalizados (paleta)~~ **Hecho** (quinta sesión). Rutas API/web siguen bloqueadas por `005`/`006` (no tocar antes).
5. ~~Implementar la UI (Filament Resources) de `001`, `002` y `003`~~ **Hecho** (commit `96bce0d` en `origin/specs/planificacion`).
6. ~~Implementar `004-solicitud-alta-taller`~~ **Hecho** (commit `c42d59e` en `origin/specs/planificacion`).
7. ~~Implementar `005-marketplace-busqueda-perfil` + `016-ui-design-system`~~ **Hecho** (commit `b31d74e` en `origin/specs/planificacion`).
8. ~~Implementar `006-resenas-favoritos`~~ **Hecho** (octava sesión, commiteado y pusheado en `e8aed6c`). Se resolvió (b) de la nota anterior: el rol `MARKETPLACE_USER`/permisos `marketplace.*` siguen sin usarse a propósito — la autorización de reseñas/favoritos es solo `auth:web`, sin capa de permisos adicional (confirma la recomendación ya documentada en la brecha estructural de la tercera sesión).
9. ~~Implementar `007-clientes-vehiculos`~~ **Hecho** (novena sesión, commiteado y pusheado en `b8802c3`).
10. ~~Implementar `008-empleados-usuarios-erp`~~ **Hecho** (décima sesión, commiteado y pusheado en `a604bd3`).
11. ~~Hotfix login Filament (email → username)~~ **Hecho** (`4da1c16`).
12. ~~Hotfix CheckSessionExpiration (string → Carbon::parse)~~ **Hecho** (`a126403`).
13. ~~Implementar `009-catalogo-servicios`~~ **Hecho** (undécima sesión, commiteado y pusheado en `76a1e7a`). Además se reemplazó el README.md default de Laravel por uno propio del proyecto (instalación, configuración, paneles, tests).
14. ~~Implementar `010-inventario-repuestos`~~ **Hecho** (duodécima sesión, 2026-07-27, commiteado y pusheado en `b7e59be`).
15. ~~Implementar `011-ordenes-trabajo`~~ **Hecho** (decimotercera sesión, 2026-07-28, commiteado y pusheado en `dbe231e`).
16. ~~Implementar `012-notas-venta`~~ **Hecho** (decimocuarta sesión, 2026-07-28, commiteado y pusheado en `d5d8f14`). Brecha de integración con `011` resuelta.
17. ~~Implementar `013-pagos`~~ **Hecho** (decimoquinta sesión, 2026-07-28, commiteado y pusheado en `e4d14fa`).
18. ~~Implementar `014-notificaciones`~~ **Hecho** (decimosexta sesión, 2026-07-28, commiteado y pusheado en `d83b268`). Sigue `015-auditoria`, última feature del orden de `AGENTS.md`.
19. ~~Al completar una feature con código + tests que cubran sus criterios de aceptación **y su UI**, actualizar `status: implemented`~~ **Hecho para 001, 002, 003, 004, 005, 016, 006, 007, 008, 009, 010, 011, 012, 013 y 014**.

## Cómo navegar si eres un agente retomando esto

Lee `AGENTS.md` primero — tiene las reglas de navegación completas. Resumen rápido: `memory/constitution.md` una vez (convenciones globales, no se repiten por feature) → `specs/<feature>/spec.md` → `plan.md` → `tasks.md` de la feature que vayas a trabajar. No leas features no relacionadas salvo que aparezcan en `depends_on`.